---
name: frontend-patterns
description: First-party frontend boundary, state, and component organization patterns.
license: MIT
metadata:
  author: pipeline-lite
---

# Frontend Patterns

Separate presentation, interaction state, transport, and domain mapping. Make component contracts
typed and explicit, keep API errors visible to users, and reuse existing project conventions before
introducing a new state or styling abstraction. Cover each interactive branch with a focused test.
