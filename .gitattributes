# OpenSpec ADRs are hash-pinned evidence. Preserve their exact final newline shape.
docs/adr/*.md whitespace=-blank-at-eof

# Canonical Skill provenance hashes are byte-sensitive; normalize every bundled asset to LF.
skills/** text=auto eol=lf
