# loop-lite progress（append-only 流水）

| 日期 | 轮 | 项 | 结果 |
|---|---|---|---|
| 2026-07-09 | iteration-38 · dashboard 全量重构（工票车间设计语言）+ G17/G18/G14 三缺口闭环 | 用户指定：重新设计整个前端（不再只是单页面），brainstorming 先行——三轮浏览器 demo（视觉伴侣）逐屏拍板：设计语言三候选选定「工票车间」（白纸双色功能语义：绿=流水线在跑，朱红=需要人出面；深绿铭牌分组头；等宽字体做结构支柱；琥珀退役）、G17 布局三方案选定「分组看板」（吸收列表方案的快捷转换按钮）、G18 纳入并动 server、G14 顺手吃掉；spec `docs/superpowers/specs/2026-07-09-dashboard-redesign-design.md` + plan `docs/superpowers/plans/2026-07-09-dashboard-redesign.md`（7 阶段 21 任务，inline 执行） | **① token 基础层**：`styles.ts` 全量重写（深浅双主题三段式机制保留、值全换；朱红一统"需要人"语义、票根虚线、`--shadow:none` 描边结构），markup 零改动先全绿 + Playwright 真机深浅色截图基线。**② G17 前端核心**：新模块 `model/workflowModel.ts`——`WorkflowRules{steps,transitions,gateByStep}` 混合模型（default 走 types.ts 常量零网络、transition-mirror 单源守卫零改动；自定义按 `fields.workflow` 走既有 `GET /api/workflows/:name` + (root,name) 缓存 + in-flight 去重）；`events.ts` 按 rules 泛化（backward=steps 序号倒退）；`inbox.ts` 判据泛化为 `gateByStep[phase]==='review'`（自定义复核门进收件箱）+ currentRoot 语境；`BoardView` 分组看板重写（每 workflow 独立分组/列集/转换图、拖拽跨组 no-op、卡片快捷转换按钮与拖拽并存、default archive 折叠计数、rules 失败组只读降级——卡不消失、折叠态 localStorage）；testid 迁移 `board-col-<wf>-<step>`/`board-group-<wf>`/`board-quick-<name>-<event>`，测试意图逐条迁移不丢。**③ G18 server（TDD 先红后绿）**：`POST /api/projects`（全仓唯一豁免第四层信任锚的写端点——职责即写注册表；补偿=路径存在+是目录+两侧规范化判重 409）+ `DELETE /api/projects` + `POST /api/changes`（init 的 HTTP 化：信任锚→name 字符集→track 枚举→loadWorkflow 先校验后落盘、自定义 phase 种首 step；preset 固定 full；server 端不写 history 登记差异）；新模块 `projects.ts`；16 新 server 用例（鉴权矩阵/落盘断言/snapshot 立即可见）。**④ G18 前端 + G14**：Nav 项目切换器（多项目下拉+计数+注册入口/单项目静态标签；App currentRoot 状态化+localStorage——吃掉 G14）；`NewChangeDialog`（名字实时校验对齐 kernel 单行 YAML 约束、workflow/track 下拉、等价 CLI 教学行、server 错误行内）；`Onboarding` 教学空状态（零项目=注册表单+CLI 复制双路径；零 change=新建引导）。**⑤ 零样式三件套**：LoopsPanel（表格→工票行+档位铭牌徽章带人话副标签+breaker 语义色去 emoji+drift 拒绝朱红条；8 条既有测试意图零改动全过）、AfkWorkbench（双栏：队列 lane 徽章+终端风日志块+操作语义归位；13 条既有意图零改动全过，含 scheduled≠running 取消门禁）、TrafficPanel（纯 CSS，testid 全保留）。**⑥ workflow 三件套收尾**：画布 gate 节点徽章（review 朱红/confirm 中性；`stepsToNodes` 注入 gateLabel 保持纯函数）+ 点阵网格底；StepDetailPanel guard 新增表单（类型下拉+条件参数 n+行内校验——补齐 G14 里"只有移除"的历史缺口，`detail_guard_add` 预留 key 终于接线）；token 兼容别名退役（grep 零消费方）。**⑦ 动效**：motion.ts 新增 `stampConfirm`（转换成功绿章 scale1.6→1）/`toastIn`/`foldOpen`（折叠方向刻意瞬时——收纳不值得让用户等编排，登记偏差），全部 150-250ms ease-out 族 + reduced-motion 瞬时；收件箱进场 stagger 只播首次数据到达。**⑧ 验收中发现并修复 G17 的 server 侧真缺口**：真机拖拽自定义卡 POST 已发出但被拒——`performTransition` 锁死在 kernel 固定事件表，自定义 event 一律 400（iteration-37 只发现了 UI 侧不可见，server 侧这半从未被触达过）；镜像 `cli/commands/transition.ts:126` 双轨分岔（读 state 按 workflow 字段分流：非 default 走 loadWorkflow+step.transitions+evaluateStepGuards；default 原链路一行不改；响应形状两轨一致=D4 端点形状零改动守住），4 新用例含 guard 拒绝零写盘。**⑨ 全量验收**（`.playwright-tmp/acceptance-redesign.mjs`，真 build+真 server 空注册表起步+真点击）：G18 闭环（onboarding 注册→表单错误态→新建 change）→ 默认 7 相位全程（快捷按钮+真 HTML5 拖拽+回退二次确认取消+盖章可见+archive 折叠计数=1；转换门槛证据字段经 CLI 播种——dashboard 是状态/决策层的既有架构判断不变）→ 编辑器建 rel（三 step+双拖线+gate+guard）→ G17 四连证（看板独立分组/draft 列卡可见/组内拖拽 approved 推进/收件箱现卡+快捷 shipped 徽章清零）→ AFK 挂队 queued 徽章 → Loops 空态 → 六视图×深浅色终版截图，**22 项检查全过、零 page error（ACCEPTANCE_ALL_PASS）**。**⑩ 八门全绿**：build+build:web+build:server / `npm test` 150 文件 1931+4skip（+16）/ `test:web` 22 文件 217（+56）/ hooks 188 / verify-skills OK / bundle 7 / oracle 0 不一致 / adapters 224。**TEST-REALITY：G14/G17/G18 改判已修**（追记块），新增 G19 登记本轮已知简化（server init 不写 history、Loops 无降档端点故 UI 不设降档钮、看板/收件箱改 currentRoot 语境、archive 折叠计数、收件箱决定类型文案行退役）。 |
| 2026-07-09 | iteration-37 · UI 重构收尾 + Playwright 全功能真机验证（正常 pipeline/AFK/自定义 workflow/loop 面板）+ 发现并处置 2 个真实缺口 | 交接会话续接：GOAL v2.0 已收官，UI 重构进行到 2/3（WorkflowEditorView/StepDetailPanel 已完成，WorkflowCanvas.tsx 未动）用户打断追加新任务清单——完成 UI + README + 用 Playwright 真实点击驱动正常 pipeline 全流程/AFK 全流程各一遍 + 建自定义 workflow 切换使用 + 结合 loop 面板验证，每小时一轮直到全部正常、无已知 bug | **① UI 重构收尾（`WorkflowCanvas.tsx`）**：接完交接文档列的 7 件事——画布舞台改用 `.workflow-canvas__stage` 承载 `<ReactFlow>` + 侧滑详情面板、三个弹窗改用 `.dialog` 系列 + `revealDialog`、保存状态 pill/工具栏/面包屑接入对应 token 类、钻入返回顶层接入此前零调用的 `crossfadeStage`；markup 改动逐一核对 `WorkflowCanvas.test.tsx` 21 个测试查询清单，`npm run test:web` 全绿未改一行既有断言。顺手修 `LoopsPanel.tsx` 一处真实 `tsc` 类型错误（`NEXT_LEVEL[row.autonomy_level]` 被重复索引两次，TS 控制流窄化不跨表达式生效）——不是本轮引入，是要真跑通 `npm run build:web` 起真 server 做后续验证时顺带发现顺手修的（`docs/TEST-REALITY.md` G15 记录的系统性缺口的一个具体症状）。**② Playwright 真机基础设施**：`npm install -D playwright` + `npx playwright install chromium`；发现写端点 token 只在真 server 的 `GET /` 同源注入到 `window.__PIPELINE_DASHBOARD_TOKEN__`，单独 `vite dev` 拿不到会 401——全程改走"真 `npm run build:web`+`npm run build:server`+真 `node packages/server/dist/main.js`"这条路径，不用 vite dev。深浅色主题下走完列表/画布/弹窗/详情侧栏/钻入拖拽连线，零 console error，UI 视觉确认通过后单独提交一次。**③ 正常 pipeline workflow 全流程真机驱动**：`demo` change 真走完 open→explore→spec→build→verify→ship→archive 全 7 相位，全部走 Playwright 真实拖拽（BoardView 原生 HTML5 拖拽用 `locator.dragTo()` 直接可用；xyflow 画布拖拽连线需要在 mousedown+小幅度 move 稳定后重新查询目标 handle 坐标，否则连不上，已验证根因是 mousedown 后 viewport 有一次性平移抖动）；额外真实演示了一次 verify-fail 回退边的二次确认对话框交互（真点确认，非假装存在）。相位所需的真实产出（design_doc/plan/tasks.md/build_mode/isolation/verification_report/branch_status/两个 review 字段/pr_url）经 CLI `pipeline set` 写入——这是有意的架构判断：dashboard 是状态/决策层不是内容编辑器，没有编辑这些字段的表单，真实场景下这些也是人/agent 用编辑器写的。**④ AFK 流程真机驱动，途中发现并修复真实缺口**：真机验证时发现 `AfkWorkbench.tsx` 只有查看快照/取消/重试，没有"挂队"入口——`pipeline afk enqueue` 此前是唯一路径，用户明确要求的"enqueue 全部真实点击驱动"做不到。按 TDD 补齐：`server/src/afk.ts::enqueueAfkRun`（镜像 automation 包 `sdk.ts::enqueue` 的判定逻辑——PM track 永不入队、默认 opt-in、非 off 态拒绝重复挂队且选择显式报错而非 SDK 侧静默幂等成功；server 对 automation 包坚持零运行时依赖，不 import，同 `CANCEL_MARKER_FILE` 字面量对位先例）+ `POST /api/afk/:name/enqueue`（路由/校验/信任锚模式逐字对齐 cancel/retry 两个兄弟端点）+ dashboard 表单（成功后 refetch+清空输入框，失败落行内错误提示）。`server.test.ts` 新增 7 例 + `AfkWorkbench.test.tsx` 新增 3 例先红后绿。补齐后真机走完 enqueue（真点击）→ 重复挂队真报错 → 查看运行中详情（sandbox/worktree/日志）→ 真点"取消"（真落 `.cancel-requested` 标记）→ 真点"重试"（真 CAS 回 queued）全链路。**⑤ 自定义 workflow 创建与切换，发现本轮最大的真实缺口**：用 workflow 编辑器 UI（真实点击）建了一个 step 名真正自定义（不是给 7 相位换皮）的 workflow `release-train`（draft→review→ship，含真实拖拽连线建两条 transition）；`pipeline init --workflow <name>` 已确认存在（不是缺口，E1 whole-branch review 时已补），但 dashboard 上确实没有"新建 change"这个入口本身（不止是 workflow 选择缺失，是整个 change 创建流程都只有 CLI 有）——判定为比 AFK enqueue 大得多的功能面（真正的表单 UX/track/preset 选择设计问题），本轮如实记录不硬造。真正意外的发现是**用这个自定义 workflow 建的 change 在看板和收件箱里完全不可见**——真开浏览器用 `GET /api/snapshot` 确认 server 端聚合完全正确（`release-demo(phase=draft)` 真在返回体里），但 `BoardView`/`InboxView` 的 UI 上一张卡都找不到。根因：`dashboard-app/src/types.ts` 的 `PHASES`/`TRANSITIONS`/`EVENT_BY_EDGE`/`REVIEW_PHASES` 四个常量只镜像 default.yaml 七相位，`BoardView.byPhase`/`InboxView.isAwaitingDecision`/`events.ts.plannedTransition` 全部基于这四个常量，任何非标准 step 名的 phase 值都会被静默丢弃或判定非法。判定为需要先设计（一块看板上不同 change 用不同 workflow 该怎么呈现列/怎么算合法转换边，是产品交互问题不是表单级小活）再实现的独立后续任务，本轮不贸然动核心看板逻辑（有把 `default` workflow 这条主路径改出回归的风险），登记 `docs/TEST-REALITY.md` **G17**（本轮最大发现）。验证目标改用其它真实可行的路径完成：`release-demo` 的相位推进改走 `pipeline transition` CLI（kernel 状态机本身对自定义 workflow 的处理不受这个 dashboard 层缺口影响，真跑验证通过——`draft-done`/`approved` 两个事件正确转换，非法事件正确拒绝）；AFK 工作台（真机验证不依赖 `phase` 值，只看 `automation` 字段）与 Loop 设置面板（loops registry 有自己独立的 `phases` 声明）经真机验证均不受影响，可以正常通过 dashboard 点击驱动。顺手登记 **G18**：项目要出现在 dashboard 里目前没有任何 CLI/UI 入口写 `~/.claude/pipeline-projects.json`，唯一方法是手改 JSON（已在 README 如实写明，非阻断性但值得后续补）。**⑥ Loop 设置面板真机验证**：读 `docs/loops/progress.md` #35/#36/#37/#38 建立 loop-engineering 全貌（registry+enforce+R1-R11+L1→L3 毕业制+budget 熔断+drift 审计）后，用真实 `.pipeline/loops.yaml` 注册一个 loop，dashboard 真机验证：查看快照表格（loop/档位/就绪分/预算/状态五列）→ 真点击展开详情（就绪分带）→ 真点击"升档→L2"按钮——**真实被 #37 漂移门拒绝**（"1 项活跃漂移未清，升档前须无漂移"），证明系统正确执行自己的治理规则而非无条件放行，这是比"升档成功"更有说服力的验证证据。**⑦ 收尾八门验证**：`npm run build`+`build:web` / `npm test`（150 文件/1915 例+4 诚实跳过）/ `npm run test:web`（19 文件/161 例）/ `tools/test-hooks.sh`（188 例）/ `tools/verify-skills.sh`（OK）/ `tools/test-bundle.sh`（7 例）/ `npm run oracle`（0 不一致）/ `tools/test-adapters.sh`（224 例，额外验证）全绿，无回归。**本轮共 4 个提交**：UI 重构（含 LoopsPanel 类型修复独立一提）、README 更新、AFK 挂队功能。**GOAL v2.0 状态不变（E/F 已全部勾满，本轮是功能验证轮非新功能开发轮）；`docs/TEST-REALITY.md` 新增 G17（自定义 workflow 在看板/收件箱不可见，本轮最大发现，已判定为独立后续任务）+ G18（项目注册无 CLI/UI 入口）。** |
| 2026-07-08 | iteration-36 · GOAL E8 workflow 编辑器画布：设计到实现全流程（9 任务 subagent-driven-development + whole-feature review + 推送 main） | 交接会话续接：E8 已完成 brainstorming + 设计文档 + 9 任务实现计划，本轮是唯一未完成事项——按 5 波并发执行实现计划、全部收尾推送 main，GOAL v2.0 清单 E/F 至此真正全部勾满 | **① 执行方式**：`superpowers:subagent-driven-development`，5 波（同波内任务文件互不重叠，真并发派发；波间有依赖，串行）：第 1 波 Task 1（kernel `serializeWorkflow`）‖ Task 4（`@xyflow/react` 依赖 + 确定性分层布局 `layoutNodes`）‖ Task 5（`WorkflowEditorView` 列表页）；第 2 波 Task 2（server GET 端点）‖ Task 6（顶层 step 拓扑画布）；第 3 波 Task 3（server POST/DELETE + `handleDelete`）‖ Task 7（钥入 skill DAG 层）；第 4 波 Task 8（`StepDetailPanel` 详情侧栏）；第 5 波 Task 9（导航接线 + GOAL.md/TEST-REALITY.md 收编）。每任务走全新子 agent 实现 + 独立审核（spec+quality 两阶段）+ 发现问题即 fix + re-review 直到批准；每波边界真跑 `npm run test:web`（+ `npm test`）确认并发无交叉污染（`i18n/translations.ts` 被多波依次追加是预期内的，不是"零重叠"）——Wave 2 边界撞见 1 次真实偶发失败（`packages/cli/src/tap.integration.test.ts` 的 SIGINT 优雅退出测试，与本次改动完全无关的 daemon 信号处理路径），单独重跑 7/7 干净、全量重跑 150 文件/1887+4 干净，判定为并发测试抢占系统资源导致的时序类偶发，非回归，未跳过验证直接采信。**② 任务级审核真实抓出的 bug（这条路径确实有效，不是走过场）**：Task 1 `depends_on:[]`（存在但空）与缺省在往返序列化时被静默混同丢弃，违反本任务自己声明的唯一正确性判据（round-trip 等价），一次修复（`!== undefined` 判断）+ 新回归测试通过；Task 2 两处 Important——404/500 分类靠子字符串匹配错误信息里的"未找到"（脆弱，可能被合法内容误判）+ `:name` 缺路径穿越守卫（Task 3 马上要复用同一套 helper 做写端点，风险更高），均修复（结构化 `WorkflowNotFoundError` 类型判断 + 照抄兄弟路由既有的 `^[a-zA-Z0-9_-]+$`+`..`拒绝模式），复审独立验证 `GateKind` barrel 碰撞声明为真实 TS2308 级冲突而非臆测；Task 5 实现方主动发现并修复 brief 示例代码本身的 bug（给定的 `confirmDelete` 用重新拉取刷新列表，与给定的无状态 fetch mock 不兼容），改乐观本地过滤，测试文件未做任何改动，复审确认这是修复 brief 缺陷而非范围偏离；Task 6 耗时最长（两个"已知坑"均需真实排查——`fireEvent.click(el,{detail})` 确认真的丢失对象改用 `CustomEvent`+回调 ref、`@xyflow/react` 的 `useNodesState`/`useEdgesState` 读真实 dist 源码验证确为 `useState` 薄封装），审核额外发现 `onNodesDelete`/`onEdgesDelete` 真实接线但零测试覆盖，两轮修复才把 edge-delete 测试的严谨性补到位（第一轮测试场景无法区分"只删一条"和"清空整个 step"，第二轮补兄弟 transition 才真正证明精确过滤）；Task 3 主动补上 Task 2 fix 引入的路径穿越守卫模式到新写路由 + 自查发现 brief 给的 POST 示例缺一个兄弟写路由都有的 body 类型校验；Task 7 已知坑诊断正确（brief 占位符选择器选错元素类型——节点而非边，因两者分属独立 DOM 子树）+ 意外发现分叉逻辑破坏了 Task 6 建立的 callback-ref 稳定性（无 cleanup 监听器堆积、新旧分支同时触发），用 `drillStepIdRef` 读最新值模式修复；Task 8 类型迁移重构（5 个本地接口迁出到 `StepDetailPanel.tsx`）+ 单击/双击 250ms 时序修复（真实浏览器 click→click→dblclick 序列用假计时器+bug-injection 验证取消行为真实生效），首次审核派发连续两次因基础设施 600s 无进展失败（非内容问题，精简指令后第三次成功），审核发现钻入 skill 层再返回顶层后详情面板会因 `selectedStepId` 从未清空而意外重开，一次修复通过；Task 9 主动核实"current root"假设（确认 App 层真的没有项目选择概念）+ 修正 brief 对 guard 新增缺口的描述（需类型条件参数渲染，非简单换 select）+ 主动发现并修复两处与刚勾选的 E8 矛盾的陈旧文档（TEST-REALITY.md G9、GOAL.md 一处状态说明），审核又发现遗漏了紧邻自己编辑的 F1 矛盾（讽刺：同一文件里两次抓到同类矛盾却漏了这一处），一次修复通过。**③ whole-feature review**（`superpowers:requesting-code-review`，opus 模型，15 commits/197KB diff，覆盖跨任务一致性/端到端功能完整性/安全性/生产就绪度，非逐任务审核的重复）：0 Critical，3 Important——(a) 事件名/字段名（`confirmConnect`/`confirmAddField`）无字符集校验但 `parse.ts` 只接受 `\S+`，用户通过正常 UI 输入含空格名字（如"design doc"）会导致保存成功（200）但下次打开 500（往返契约破坏，文件完好但打不开），真实可复现的 bug 非理论风险；(b) `WorkflowEditorView` 的 DELETE 失败复用"加载失败"致命错误态，把整个列表+新建表单清空成一行文字且无恢复路径；(c) 系统性发现（非 E8 引入，E8 过程中被两个不同任务实现方独立发现）：`dashboard-app` 自己的 `tsc --noEmit` 从未被根 `npm run build`/CI 真正跑过，用预先存在的 `LoopsPanel.tsx:117` 类型错误作实据，E8 新增组件本身类型干净。均已处理：(a)(b) 派一次合并 fix（不逐条各自派），复审员亲自逐行手推 `parse.ts` 正则/控制流证实"往返损坏"回归测试是真实闭环而非断言，同时发现 fix 自身披露的残留窄口子（kernel `validateWorkflow` 仍无服务端字符集校验后盾，今天就可通过一次已鉴权直连 HTTP 触发，非纯假设）登记 TEST-REALITY.md **G16**（本轮不做，原始审核明确标注服务端加固为"可选"）；(c) 登记 **G15**（本轮不处理，超出 E8 范围）。修复后重新完整跑一遍八门验证仍全绿（test:web 154→158，含新增 4 例回归测试）。**④ 文档收编**：GOAL.md E8 勾选（含准确的已知简化点：多项目场景固定编辑 `snapshot.projects[0]`、guard 只支持移除不支持新增、不支持撤销重做多选 minimap、改名需删除重建——均对照实际落地代码逐条核实，非照抄计划草稿）；TEST-REALITY.md 新增 **G14**（E8 已知简化点，Task 9 登记）+ **G15**（dashboard-app 类型检查系统性缺口）+ **G16**（kernel 字符集校验服务端后盾缺口）三条诚实缺口；GOAL.md F1 条目、TEST-REALITY.md G9 两处随手修正为与 E8 已完成一致。**⑤ 收尾推送**：`superpowers:finishing-a-development-branch`——`main`（5eff5df）`git merge --ff-only worktree-workflow-editor-canvas` 干净快进（无需 merge commit，main 自本 worktree 分出后从未移动），合并后在主仓库重新 `npm install`（`package-lock.json` 因新增 `@xyflow/react` 依赖有变）+ 完整重跑 build/vitest/test:web 确认绿，`git push origin main`（5eff5df→abf6e14，18 commits）。**八门全绿收官**：vitest 150 文件/1907+4honest-skip（较 iteration-35 净增 42，含本轮全部 kernel/server 新测试）、前端 19 文件/158（16→19 文件，净增 layout/WorkflowEditorView/WorkflowCanvas/StepDetailPanel 四个新测试文件）、hooks 188、adapters 224、verify-skills OK、bundle 7/7、oracle 0 不一致。**GOAL v2.0 状态：清单 E（E1-E8）与清单 F（F1-F4）至此全部勾满，v2.0 完整收官。** |
| 2026-07-08 | iteration-35 · GOAL v2.0 集成收尾：四份计划合并 + F1 导航 + whole-branch 终审 + 推送 main | 交接会话续接：四份实现计划（afk-workbench 8/8、loop-settings-dashboard 5/5、skill-editor-transfer-modal 4/4 已合并进 feature/dashboard-workbench；workflow-customization-engine 11/11 待合并）在任务层面全部完成并逐任务审核通过，本轮做集成收官——迁移进 main 之前唯一没做过的是"四份计划合在一起之后"这个总览层级的验证 | **① 合并**：`git merge --no-ff feature/workflow-customization-engine` 进 `feature/dashboard-workbench`，零文件重叠、真跑无冲突（c6d0bc7）；随后清理三个已消费完的 worktree（.worktrees/loop-settings-dashboard、skill-editor-transfer-modal、workflow-customization-engine），有价值的 `.superpowers/sdd/progress.md` 内容摘录进本条目再删。**② 八门验证**：build/vitest（147→148 文件）/test:web/test-hooks/test-adapters/verify-skills/test-bundle/oracle 全绿，merge 后不是假设"零重叠=零冲突"而是真跑了一遍。**③ GOAL.md F1 导航收尾**：`Nav.tsx` 新增 `WORKBENCH_VIEWS=[loops,afk]` 挂在"工作台"下拉分组（点击展开/选中收起/子视图活跃时触发器自身标 aria-current），顶部恢复到 3 项（收件箱/看板/设置）+1 分组触发；`AdvancedPanel.tsx` 同步把 `loops` 从 TOOLS 降级列表里摘掉（同 Task 8 当时对 `afk` 的处置——真实视图存在时不再重复渲染只读摘要）。**④ 文档回写**：GOAL.md E1-E7/F1-F4 逐条核实证据后勾选（E8 画布 UI 如实标注"故意不做"，不勾；E1/E3/E6 三处发现真实的实现偏差并如实脚注，非静默勾掉——E1 是 `workflow`+复用 `phase` 而非新增 `current_step` 字段、E3 是 `handoff.ts` 未接 step inputs/outputs（设计文档本身就没提这茬，非实现偷工）、E6 是 gate.sh 仅在非 default workflow+Skill 调用时窄例外 spawn node）；CONTRACT.md §5.4 回写该窄例外；TEST-REALITY.md 登记本轮 6 条已知诚实缺口（G7-G12，源自四个 progress.md）+ 1 条本轮新发现（G13：E3 handoff.ts 未接线）。**⑤ Whole-branch review（本轮首次、也是本项目第一次对四份计划合并结果做总览终审）**：2 个独立 opus 评审并行——通用评审（plan alignment/code quality/architecture/testing/production readiness）+ 跨计划集成风险专项（server.ts 三计划路由共存一致性/Nav.tsx+App.tsx 合并后真实 UX/跨面板命名与错误处理风格/i18n 完整性/自定义 workflow 与既有 dashboard 代码的交互面），均独立重跑八门相关验证复核绿、独立核实 E1/E3/E6 三处披露偏差属实。**0 Critical，5 条 Important 全部修复**：(a) E5 `validateWorkflow` 零生产调用方——接入 `loadWorkflow`，修复中发现并顺带修正两处被这条缺失校验掩盖的真实 fixture bug（`internal-skill-gate-hook.integration.test.ts` 的 `depends_on` 引用同 step 内未声明 skill、`internalSkillGate.test.ts` 中间 step 缺 transitions 走死路），两处修复均不改变原有断言；(b) 自定义 workflow 无支持入口把 change 摆到首个 step——新增 `pipeline init --workflow <name>`（真加载校验后种 `phase=steps[0].id`，绕开 CLI `set` 的 manifest 枚举闸、zero 触碰 oracle 覆盖的 enumOk/cmdSet 共享路径），两个原本手改 `.pipeline.yaml` 的 workaround 测试改走真实支持路径；(c) `POST /api/loops/level` 逻辑拒绝仍返回 200（graduation 判定跨级/未就绪时 exitCode 非 0 但 HTTP 200，前端 `res.ok` 检测不到）——改 `exitCode===0` 才 200，对齐 cancel/retry 两个兄弟端点；(d) `LoopsPanel`/`SkillTransferModal` 缺 `r.ok` 检查——server 错误统一走 JSON 信封，非 2xx 时 `r.json()` 仍会 resolve，无检查等于报错永远不触发、无 ErrorBoundary 会白屏，补检查复用 `AfkWorkbench` 已有的 `readErrorDetail` 模式；(e) `AfkWorkbench`/`LoopsPanel`/`SkillTransferModal` 三个面板完全不走 i18n（切到英文一级导航是英文、内容仍是中文）——补齐 `afk`/`loops`/`skill_transfer` 三个双语命名空间。顺手处理的 Minor：取消/重试/升档成功后补 refetch（此前成功也不刷新，界面停在操作前状态）；AFK 详情侧栏补渲染 sandbox/worktree/last_error（数据已 fetch 但从未渲染，F3/design §4 明确要求）；删除确认无引用的死代码 `AdvancedPanel`/`AfkPanel` 三件套（.tsx+.test.tsx+afkData.ts）；`/api/change/.../transition` 信任锚校验统一为 `dedupeRoots`（原 inline Set 对注册表空字符串条目会解析成 cwd 当可信条目）。**⑥ 收尾推送**：`superpowers:finishing-a-development-branch`——`main`（83457cd）`git merge --ff-only feature/dashboard-workbench` 干净快进（无需 merge commit，main 自四条 feature 分支拆出后从未移动），merge 后完整重跑 build+vitest 确认绿，`git push origin main`（83457cd→7f3804e，79 commits），删除已合并的 `feature/dashboard-workbench` 分支（其余三条已消费完的 feature 分支留作本地参照，其内容已完整包含在 main 历史里，删不删都不影响任何东西）。**八门全绿收官**：vitest 148 文件/1865+4honest-skip（较交接时 1409 净增 456，含本轮新增的 loadWorkflow/init-workflow/internal-skill-gate-hook 等真 fs e2e）、前端 115（16→15 文件，净删 AfkPanel 4 例、净增本轮修复相关用例）、hooks 188、adapters 224、verify-skills OK、bundle 7/7、oracle 0 不一致。**GOAL v2.0 状态**：F 全绿（F1-F4）；E 除 E8（画布 UI，故意留给后续独立计划）外全绿。 |
| 2026-07-06 | iteration-0 | T1 契约与骨架 | 收编：GOAL/LOOP/BACKLOG/CONTRACT/plan + workspaces + types.ts |
| 2026-07-07 | iteration-34 · CI 首次真跑抓出 hooks 全仓 8 处 stat -f/-c 跨平台真 bug 并修复 | iteration-33 推送后 CI 第一次在真 ubuntu-latest 上跑，`vitest` 步骤当场红：新增的 G4 e2e（workflow-skill-orchestration.integration.test.ts）在真跑「新鲜 review marker 应拦截」「陈旧 marker 应放行」两处均报 `gate.sh` 返回非 0/2 的 1（而非预期值），暴露此前从未在 Linux 上跑过测试的真实空白 | **根因**：`hooks/` 全仓 8 处（gate.sh/skill-tracker.sh/router.sh/breadcrumb.sh/statusline.sh×2/session-start.sh/decision-recorder.sh）同一句 mtime 读取写法 `stat -f %m "$f" 2>/dev/null \|\| stat -c %Y "$f" 2>/dev/null \|\| echo ...`：GNU `stat -f` 是「文件系统状态」模式（非文件 mtime），传真实文件路径不报错、反而**成功**吐一整段文件系统信息文本，导致 `\|\|` 退出码兜底永不触发到正确的 `-c` 分支；`gate.sh` 拿这段垃圾文本去 `$((now - mt))` 算术直接 `arithmetic syntax error` 崩溃退出 1。本机 macOS（BSD stat 天然认 `-f`）从未暴露过，本仓在 iteration-33 补 CI 之前也从无 Linux 真跑记录——这正是被"没有 CI"这一操作性空白本身掩盖的真实缺口，补 CI 当轮即真抓出。**修复**：8 处统一改写为「先试 GNU 语法（`-c`；BSD stat 不识别该 flag 会真报错退出，兜底方向不受影响）+ 对拿到的值做纯数字校验，而非只信退出码」。**验证方式**（未再走一轮慢速 CI 才发现，而是本机用真 Ubuntu docker 容器直接验证）：① 用 `docker run ubuntu:latest` 直接复现老写法在真 GNU coreutils 下崩溃（`stat -f` 真吐出多行 `Block size/Inodes/...` 文本，`$(( ))` 对着它真报 `arithmetic syntax error`）；② 验证新写法在同环境下正确产出纯数字 epoch 且算术干净；③ 挂载真 `hooks/gate.sh` 进同一 Ubuntu 容器，直接跑两条真实场景——新鲜 marker 真返回 exit 2 拦截、陈旧（回拨 1 小时）marker 真返回 exit 0 放行且真自清 marker 文件——逐字复现并验证修复了 G4 在 CI 上报错的两个具体场景。本机 macOS：test-hooks.sh 180/180、vitest 134 文件/1745+4skip 全绿（无回归）。提交推送后 CI 真跑通过（ubuntu-latest 实测绿，非仅本机声称）。**教训沉淀**：全部 180 个 test-hooks.sh 断言、全部 hooks 相关 vitest 此前只在 macOS 本机跑过，从未在 Linux 上验证——"跑在我机器上"≠"跨平台正确"，这也是本项目第一次给自己补 CI 就抓出真实生产缺口的直接证据（若无这轮 CI，任何 Linux 用户装上这个插件都会在门 marker 首次判新鲜时真崩溃）。 |
| 2026-07-07 | iteration-33 · 长尾挂账清零：G4/G5 + 5 长尾适配器 + dashboard config 端点 + CI + 镜像发布文档 | 用户指令"接下来的全部执行"——一次性处理交接文档列出的全部候选项：4 项互不相交的工作用并行 agent fan-out（依 `superpowers:dispatching-parallel-agents`），CI + 镜像发布脚本主会话直接做 | **G4（`packages/cli/src/workflow-skill-orchestration.integration.test.ts`，4 例零 mock）**：真跑一个项目走完整 7 相位（open→explore→spec→build→verify→ship→archive），每相位真驱动 `hooks/*.sh` 子进程（`gate.sh` 硬拦→真 `AskUserQuestion` 解锁→`skill-tracker.sh`/`interactive-skill-gate.sh` 真记账），mandatory_skills 从 `templates/manifest.yaml` 真读（非手选）；单一 `.pipeline-history.jsonl` 里 kernel 写入与 hook 写入按**因果顺序**逐条核验（非仅计数）；变异测试自证非空转绿（强制 gate 恒放行→抓出具体真失败→复原回绿）。**G5（`packages/kernel/src/mem/adapters/opencode.ts`，19 真例）**：改用 `node:sqlite` 内建模块真读 OpenCode 会话（零第三方依赖，替换此前因 better-sqlite3 装不上而回退的 no-op 桩）；schema 非凭源码猜测——真跑 `opencode-ai@1.17.14` 建库+真会话+`sqlite3 .schema` 逐字核对；发现 `node:sqlite` 在 node 22.5–22.12 需 `--experimental-sqlite` 标志（本仓 engines 仅要求 >=22），故用 `try/catch` 每次调用探测、失败诚实降级空结果而非缓存"不可用"；诚实缺口：compaction 边界摘要暂未折叠（OpenCode 摘要落在独立 message，无真实压缩会话可核对前不猜规则）。**5 长尾适配器（aider/continue/cline/amp/zed）全部真实现**：非照抄 registry.yaml 已登记档位——continue（真查证是现代 CLI `cn` 而非旧 IDE 插件，其 hook schema 自陈"沿用 Claude Code 同款"）与 cline（真读其开源 `hooks.proto`/`.clinerules/hooks/`，证实真硬拦而非审批流）均由目标档 B 升级为 A；amp 经真下载官方二进制 `strings` 分析验证机制不同构但能力等价，仍判档 A；zed 经真实 GitHub issue 确认无 hook 原语，档 C 不变；conformance 125→224，变异测试覆盖 cline/amp/aider 三项；顺带修复 `tools/test-adapters.sh` 一个真实预置 bug（`drive_track()` 单语句内变量展开顺序错误，此前仅因调用点恰好都在同名 for 循环里才"能用"）。**Dashboard 配置写端点**：`GET /api/config`+`POST /api/config/mandatory-skills`（`packages/server/src/config.ts`），精确复用既有 B5 token 鉴权（同一 `handlePost` 派发链，零新鉴权代码）；写入对 `templates/manifest.yaml` 的 `mandatory_skills:` 块行级手术式替换，写临时文件后真过一遍 kernel `loadManifest` 回读校验才 `rename()` 覆盖；phase/track/skill token 全白名单校验；`SettingsView.tsx` 按 `capabilities.config` 优雅降级。server 89/89、dashboard-app 86/86。**CI**：`.github/workflows/ci.yml`（ubuntu-latest 自带 docker，八门真跑非仅子集）。**镜像发布**：`tools/sandcastle/build.sh`+README 记录手动构建+发布步骤，未代为选择 registry 或实际推送（留仓库所有者决定）。**集成阶段**：4 agent 均严格遵守文件所有权、互不冲突，仅 1 处需主会话补线（`sessions.ts` 的 `opencodeSearch(kw)`→`opencodeSearch(fs,s,kw)` 一行，因该文件不在任何 agent 的所有权范围）；集成时抓出 1 个真回归——vitest 通过 `@pipeline-lite/kernel` 消费 kernel dist 时命中一次 vite 依赖预构建缓存过期（`node_modules/.vite` 未随 kernel 重新构建失效，纯 node 直接 import 验证过 dist 本身正确），清缓存后现出 1 处真实需要更新的旧测试断言（旧测试硬编码"OpenCode 警告总是打印"，与 G5 修复后的真实条件行为冲突，已按新行为改为条件断言）。八门全绿：vitest 134 文件/1745+4skip、前端 86、hooks 180、adapters 224、verify-skills OK、bundle 7/7、oracle 0 不一致。**无新增诚实缺口，交接文档列出的全部候选项一次性清零。** |
| 2026-07-07 | iteration-32 · G6 闭环：有效 token 真跑 full CC-in-sandbox，agent 真编码成功 | 交接会话续接：上一轮（iteration-31）token 被 Anthropic 判 401，agent 编码这一步未验证；本轮拿到有效 `sk-ant-oat01-...` token（用户先给的是 OAuth 登录流程的 authorization code `code#state`，非最终 token，逐层排除后才定位——host 直跑+`env -i` 清空环境的最简化对照实验证明问题不在本仓代码/环境），真跑到底 | **凭证定位**：最简化对照实验（宿主机直跑 `claude`、`env -i` 清空环境、绕开 docker/tap/repo 代码）逐层排除 shell 转义（`nodeExec` 走 `spawn()` 数组参数，全程无 shell）、docker/tap 链路、本机残留登录态，定位到用户最初给的字符串是 OAuth 授权码而非兑换后 token；换到真 `sk-ant-oat01-...` token 后host 直连测试先行通过。**真跑抓出并修的 2 个此前从未被有效凭证触发过的沙箱环境真缺口**（均是"首次有真 token 走到 agent 真调用这一步"才可能触发，之前 401/无 token 的路径从未真正到达）：① **alpine 缺 bash、`SHELL` 未设**——Claude Code Bash 工具报 `No suitable shell found`，agent 完全无法执行命令，诚实报告卡住而非伪造结果；修 `tools/sandcastle/Dockerfile` agent 层追加 `apk add --no-cache bash` + `ENV SHELL=/bin/bash`（置于 npm install 层之后不影响其缓存）。② **容器 `--user host-uid:host-gid` 在 alpine `/etc/passwd` 查不到条目时 `HOME` 默认解析成 `/`**（非空、非未设——原 `${HOME:-/tmp}` 兜底判断"未设或空"，捕不到"设了但设错"，真跑实测才发现），agent 建 `~/.claude` 时 `EACCES: mkdir '/.claude'`；修 `pipeline-afk-run.sh` 改无条件 `export HOME=/tmp` + 按当前 uid 自助注册 `/etc/passwd` 条目（幂等），Dockerfile 配合 `chmod o+w /etc/passwd /etc/group` 放行非 root uid 写入。**两轮修复后真验证结果**：一次性诊断脚本（未保留在仓库里）seed 一个带真实 `design_doc` 的 change → `createAutomation`+`createDockerRunChange`（L1，`extraEnv` 注入 token）→ agent 真读设计文档、真建 `HELLO.md`（内容逐字比对："hello from g6 agent"）、真 `git commit`（`edf75df`），`git show` 独立核验而非只信 agent 自报；tap 记录 8 条真请求，逐字确认 `upstream_base_url: https://api.anthropic.com` + 真 `claude-cli/2.1.202` User-Agent + 真 `anthropic-beta: oauth-2025-04-20`（OAuth 认证专属 beta 头）+ `response.status: 200`，"走代理不直连"约束再次实证成立；`automation` 字段正确落 `paused`（L1 安全默认，未误判为 merge）。八门全绿：vitest 1688+4honest-skip、其余不变（Dockerfile/pipeline-afk-run.sh 改动不影响任何既有测试断言，已逐一确认无测试依赖其精确内容）。**安全处置**：token 全程只作临时环境变量传递给单次调用，未写入任何文件/commit/memory；一次性诊断脚本与所有临时 host 仓库（含 tap 捕获的凭证痕迹）验证后已清理。**G6 缺口彻底清零，GOAL 全部四张清单 + 部署接线均已闭环，无遗留诚实缺口。** |
| 2026-07-07 | iteration-31 · 真 token 验证 full CC-in-sandbox，抓出 3 个真缺口并修复 | 用户提供真 CLAUDE_CODE_OAUTH_TOKEN，要求真跑 full agent-in-sandbox 且必须走 tap 代理（不直连）——这是 iteration-30 仍诚实 honest-skip 的最后支线，本轮拿到真凭证后真跑到底 | **抓出并修的 3 个真缺口**（全部是此前 `#29-wire`/`#34-wire` 从未被真实凭证/真实网络约束触发过的死角）：① **extraEnv 通道缺失**——`runChangeInSandbox`/`createDockerRunChange` 此前只硬编码注入 `PIPELINE_AFK=1`，没有任何机制能把 token/代理地址传进沙箱，之前 4 处 docker honest-skip 从未触到这个缺口；补 `RunChangeConfig.extraEnv`/`DockerRunChangeOptions.extraEnv`（真流到 `docker run -e` argv，2 新单测 + lifecycle.test.ts 1 新用例）。② **`host.docker.internal` 在本沙箱化环境里对宿主监听端口 silently drop**（非"connection refused"，是真超时——用真 http server + 真 docker run 探测过，DNS 能解析但 TCP 连接被吞）：放弃"host 起 tap、容器连回 host"方案，改**容器内自起 `pipeline tap start`**（同网络命名空间，零跨容器路由依赖，任何 docker 环境下都成立，不依赖这台机器的特定网络实现）——这也是 `pipeline tap start` CLI（#34-wire 新增）第一次在生产场景里被自己的沙箱镜像消费，验证了它作为"可嵌入其它脚本的真实 daemon 启动器"这个定位。③ **pipeline-afk-run.sh 的 agent 分支此前从未真跑过就会挂死**——headless 容器无 TTY 应答权限确认，缺 `--dangerously-skip-permissions` 每个工具调用都会挂死；补上该 flag（Anthropic 文档原话："recommended only for sandboxes with no internet access"——本沙箱因为要连真 API 必然有网络，此为真实、非新增的风险点，非本轮引入，如实记录）+ `timeout 300`（busybox 自带 applet）兜底 + 日志/tap trace 落 worktree 内目录（随 git commit 进命名分支，teardown 后仍可 `git show <branch>:<path>` 读到）。**Dockerfile 顺带重排层序**（npm install 8 分钟这层挪到脚本 COPY 之前，此前脚本一改就得重装一遍 npm 包，缓存形同虚设）。**真验证结果**：容器→（容器内）tap reverse proxy→真 `api.anthropic.com` 全链路真跑通——tap trace_store 真记录 4 条完整请求（含真实 claude-cli/2.1.202 User-Agent、真实系统提示词、真实 `Bearer sk-an...` 头），证明"走代理不直连"这个用户明确要求的约束真实成立；但该 token 被 Anthropic 真实服务端拒绝（`401 Invalid bearer token`，非本仓代码问题，也未消耗真实 API 额度）——**agent 编码这一步仍未验证成功**，诚实记录待用户提供有效 token 后再验证；`automation` 字段正确落 `paused`（L1 安全默认，未误判为成功）。八门全绿：vitest 1688+4honest-skip、其余不变。**安全处置**：token 只作为单次 Bash 调用的临时环境变量使用，从未写入任何文件/commit/memory；因已在对话中明文出现且被服务端拒绝，建议用户视为已泄露，重新生成后走更安全的传递方式。 |
| 2026-07-07 | iteration-30 · #29-wire + #34-wire 部署接线双双翻真跑 | 交接会话续接：两项 v1.0 收官时标注的 env-gated 部署接线，本轮拿到真 docker 环境后全部真跑验证，不再是占位 | **#29-wire**：`tools/sandcastle/Dockerfile`+`pipeline-afk-run.sh` 真镜像（按脚本实际需要精简到仅装 git——apk 在本环境奇慢，python3/jq/bash 全裁掉后构建从 10+分钟降到 ~35s；真踩坑：docker build 日志显示 python3-pycache-pyc0 编译标准库单步耗时 350+ 秒，才发现是过度按老仓工具链照单全收，而非环境真的那么慢）+ `automation/src/sdk/dockerRunChange.ts`（createLifecyclePorts+runChangeInSandbox 装配成 RunChange，autoMerge 严格随 level 派生）+ `lifecycle/ports.ts` 补挂 worktree 工作目录本身（此前只挂 .git，容器内 git commit 会因看不到工作文件而失败——这是全新 e2e 才会踩到的坑，此前 4 个 docker honest-skip 从未真跑过这条路径）；`pipeline afk run` 从纯 report 改真调 `automation.runRound(createDockerRunChange(...))`（`--level`/`--image` 新 flag，无 docker/非 git 仓诚实降级不伪装）。dockerRunChange.integration.test.ts 3 例（L3 真容器+真 merge-back+真 barrier build_sha、L1 report-only 真验证不 merge）+ afk-run.integration.test.ts 4 例（CLI 层同款验证，自足建镜像不依赖跨文件执行序）；docker.integration.test.ts/container.integration.test.ts 原 4 处 honest-skip 中 2 处（非 token 门控）翻真跑，剩 2 处 CLAUDE_CODE_OAUTH_TOKEN 门控诚实保留。**#34-wire**：daemon.ts 接 `ca?: CertificateAuthority` 透传给 forward 绑定的 `serveForward({ca})`（2 真 TLS MITM 例证明走 daemon 装配而非直调）；全新 `launch.ts`（`planBindings`+`launchTap`）用 detectTarget+reverseEnvMap/forwardEnvMap 真装配一次可跑的 daemon（forward client 缺 `opts.ca` 直接拒绝而非静默盲隧道——避免 CA 信任 env 指向不存在路径）；forward-proxy.ts 响应体接 `decodeBedrockEventstreamEvents`+`assembleBedrockConverseBody`（真解码 AWS EventStream，替代此前的乱码 safeJson）；**全新 `ws-proxy.ts`**：`WsFrameAccumulator` 增量帧合并（跨 TCP 分片正确重组 continuation）+ 真中继 wss:// upgrade（CONNECT→TLS MITM 终结之后的 upgrade 事件，透明转发握手+帧字节，同时旁路解帧）→ 首次把 #34b 建好但从未接线的 `reconstructWsRequestBody`/`reconstructWsResponseBody`/`isPromptBearingWsRequestBody` 接入活路径（prompt-bearing 门控 + capture OFF 门控，7 例真 CONNECT+TLS+WS 握手+帧中继）；security.ts `InterceptEntry.tls` 供 doctor「正在解密」披露。**全新 `pipeline tap start` CLI**（tap 包此前零 CLI 可达性）：daemon 模式前台常驻打印 export 行、SIGINT/SIGTERM 真关 daemon；`-- <command>` 模式真 spawn 注入 env、子进程退出即收尾。**意外收获**：真 e2e 抓出 commander(^12.1.0) 一个真实 bug——variadic `[args...]` 里裸 `--`，若前一 token 是普通位置参数会被静默吞掉，若前一 token 是 `--foo` 形态则保留（受控 argv 数组穷举验证），main.ts 改为从原始 process.argv 手工切出 passthrough 段绕开（`deps.passthroughArgv`），commander 自始至终看不到裸 `--`。七门全绿：vitest 1685+4honest-skip（原 1652+4skip，2 处 docker honest-skip 翻真跑抵消 automation/cli 新增用例的 skip 计数，净增 33 例）、前端 80、hooks 180、adapters 125、verify-skills OK、bundle 7/7、oracle 0 不一致。**GOAL A5/A7 从「代码达标仅缺部署接线」升级为「部署接线真跑验证」；唯一剩余诚实缺口收窄为 CLAUDE_CODE_OAUTH_TOKEN 门控的 full agent-in-sandbox 编码支线（4 处 honest-skip，需真实部署凭证，非代码/非环境缺口）。** |
| 2026-07-07 | iteration-29 · 收敛复查补两缝隙 | /loop 再触发——诚实复查抓出 2 个真实可执行缝隙（非环境门控） | ① **afk CLI 命令缺失**：#29 automation 包全测试但 cli 零可达性（package.json 都没依赖）——补 `pipeline afk enqueue/scan/status/run`（消费 automation SDK createAutomation），enqueue 真落 automation=queued+queued_at、PM 轨真拒、run 诚实指向部署接线 #29-wire 不伪装 docker；cli 加 automation dep + tsconfig ref + 根 build 顺序修正；6 真 fs e2e。② **dashboard 第三份 transition 镜像**（#25b 只修 cli+server）：浏览器 bundle 不能 import node-native kernel，故加**跨边界单源守卫测试**（node 侧真 import kernel TRANSITION_EVENTS + dashboard 镜像逐边字节相等，漂移即抓红）——同 #25b 对 server 的引用相等断言，此处是数据相等。七门全绿：vitest 1652+4skip、前端 80、hooks 180、adapters 125、verify-skills OK、bundle 7/7、oracle 0 不一致。**复查确认：除 2 个 env-gated 部署接线外无遗留可执行缺口，v1.0 收敛稳固。** |
| 2026-07-07 | **iteration-28 · v1.0 收敛收官** | loop-lite kill 判据满足：功能 backlog 清空 | **收敛检查全绿 → 宣告 v1.0 收官。** GOAL 四张清单全部达成：清单 A 功能完备（A0-A7 全收编，8 大子系统核心+协议面）· 清单 B 优化点（B1-B21 全勾，架构/UI/竞品/loop-engineering 四组）· 清单 C 质量保障（C1-C10 全守，含用户硬要求的 C9/C10 无伪测试）· 清单 D 竞品超越（9 维 > / 5 维 ≥ / 3 护城河）。**规模**：6 workspace 包（kernel/cli/server/dashboard-app/automation/tap）+ adapters 框架（7 active 平台）+ 纯 bash hooks；**测试**：vitest 1644+4honest-skip / 前端 jsdom 80 / hooks 180 / adapters conformance 125 / bundle 冒烟 7 / **golden-oracle 双跑 0 不一致（每轮）**≈ 2040 断言，零伪测试（真 fs/HTTP/socket/进程/crypto/git e2e）；36 commits，全部推送。**唯一剩余**：2 个 env-gated 部署接线（#29-wire docker 镜像装配 / #34-wire daemon TLS 绑定）——需 docker daemon + 预构建 sandcastle 镜像 + 运行中 tap daemon 才能验证，是真实部署环境门控、非代码缺口，代码侧均已完成且经真测（automation docker IT honest-skip、tap TLS 在 node v24 真跑）。里程碑：M1 内核深度✅ M2 hooks/插件✅ M3 dashboard✅ M4 channel+mem✅ M5 automation✅ M6 竞品缺口✅ M7 平台矩阵✅ M8 tap✅ M-loop 治理闭环✅。**loop-lite 终止（连续收敛无新缺口）。** |
| 2026-07-07 | iteration-27 | #40 平台铺量 + 超越矩阵校准 → **清单 D 全绿** | #40：registry planned 的 gemini(A全保真)/copilot(B inject降级)/pi(B veto降级)/devin(C全静态) 4 平台转 active——各建适配器目录（三能力按档位 native 或如实降级）+ registry 能力矩阵填满 + 顶层 install.sh 派发；**longtail 5 平台登记**（aider/continue/cline/amp/zed + 目标档位，不硬凑 padding）。conformance §⑧ 67 新断言（veto 逐场景等价 baseline gate.sh、inject 真含宪法、track 真 append JSONL 逐字对齐、degraded 平台断言无伪原生 hook）+ **真文件变异测试**（改坏 gemini veto→2 DENY 场景抓红、还原回绿）。active 平台 3→7 跨 A/B/C 三档、3 个不同降级点。三门全绿：test-adapters 125/0、verify-skills OK、hooks 180/0。超越矩阵校准：D7/D14 更新为 active 7 + conformance 机器校验 > 竞品手工投影；D4/D16 修正为已闭环。**GOAL 清单 D 全绿：9 维 >（D2/D4/D7/D8/D9/D10/D11/D12/D14）+ 5 维 ≥（D1/D3/D5/D6/D13）+ 3 护城河（D15/D16/无伪测试）——"任何方面都 ≥ 且核心维度 >"成立。** 剩余仅部署门控接线（#29-wire docker 镜像 / #34-wire daemon TLS，env-gated 非代码缺口）。 |
| 2026-07-07 | iteration-26 | 并行批次 15 收编（#34b/#34c tap 协议面 + #29c automation docker 全链）→ **M5/M8 代码达标** | #34b/#34c：ws-reconstruct（RFC6455 帧真掩码 XOR/续帧合并/body 重组）+ bedrock（AWS EventStream 二进制解码 + 内置纯 JS CRC32 与 node:zlib 交叉校验）+ certs（**纯 node:crypto 替老仓第三方 cryptography**：RSA + 手写 ASN.1 DER + 真自签 + X509 真验签）+ clients（13 runtime：codex/kimi/openclaw/codebuddy/gemini 等 reverse/forward 路由）；**本地 CA 私钥四重护栏**（本地生成 + certs 零 outbound 源码扫描 + 0600 断言 + 信任只构命令不自动改钥匙串）；TLS MITM 双护栏（仅 ca+capture ON 才解密，默认 OFF 盲隧道）；60 测试 node v24 真跑 0 honest-skip（真 CONNECT→CA 签 host 证书→TLS 终结→解密落 trace）。#29c：docker 全链真实现（node:child_process 真 docker run/exec/rm 注入面）+ 真 git worktree add/remove + merge-back DELIVERY 守卫 + **冲突真留现场**（SyncError→worktree 不清 + preserved_path，真 git 冲突 IT 实证 base 干净回退+worktree 仍在）+ race idle/grace/abort + boundedTail 64KiB + git 双挂载；111 测试（36 真单测 + 3 真 git IT 真跑 + 4 docker honest-skip 无 docker 不伪绿）。收编接线：模块完成且真测证明；跨包生产接线（cli docker 镜像装配 #29-wire、daemon TLS 绑定 #34-wire）记为部署门控后续（env-gated，非代码缺口）。七门全绿：vitest 1644+4skip、前端 80、hooks 180、adapters 58、verify-skills OK、bundle 7/7、oracle 0 不一致。**A5/A7 代码达标勾——8 大子系统全部核心+协议面完成。** 剩 M7 #40 平台铺量。 |
| 2026-07-07 | iteration-25 | 并行批次 14 收编（#27b channel 进程层 + #29d/#34d server 数据端）→ **M4 完成** | #27b：channel 进程管理层——supervisor 三循环 + 真 fork 子进程（cat -u 桥接）+ inbox_watcher 增量 live-tail + OS-liveness 四重判定（os.kill(pid,0)+ps cmdline 正则防 pid 复用）+ ShutdownController 幂等漏斗 kill ladder（closeStdin→SIGTERM→SIGKILL）+ cleanupExpiredIdleWorkers 从不自写 killed；cmdChannel spawn/kill/run/prune 真实现替换 exit2 占位；**架构红线自证**（跑完零 openspec/门 marker 不变、throwing-proxy 守 store/flow）；93 测试含 11 真进程 e2e。#29d/#34d：server /api/afk/snapshot（6 泳道 + 调度器 doctor 灯）+ /api/afk/log + /api/traces/sessions + /api/traces/records，capabilities 声明驱动 dashboard Advanced 面板真消费；14 真 HTTP + 13 真 render。收编接线：server 注入 @pipeline-lite/tap createTraceStore（traffic 上线，capture 默认 OFF 时空会话）+ tsconfig references + package.json dep；channel 命令 iteration-18 已注册。七门全绿：vitest 1545+2skip、前端 80、hooks 180、adapters 58、verify-skills OK、bundle 7/7、oracle 0 不一致。**M4 channel+mem 里程碑完成（A4 勾）。** 遗留：#29c docker 全链（需 docker）、#34b ws/CA、#34c 多 runtime、#40 平台铺量。 |
| 2026-07-07 | iteration-24 | 并行批次 13 收编（#25b transition 单源 + #38 L1→L3 毕业制）→ **D16 loop 治理闭环** | #25b：transition 事件表 + 前置校验 + 副作用上提 kernel/flow/transition-table.ts 单源，cli events.ts/transition.ts + server transition.ts **净删 303 行镜像**、改为共消费 kernel（含 build-complete 未取 HEAD 的 WARN 差异经 buildShaMissing 信号保留）；**行为保持重构铁证：oracle 双跑 0 不一致 + 现有测试全绿 + bundle 全程 transition 跑通**；40 kernel 单测作新单源回归锚。#38：graduation.ts 升降档裁决——升档准入(L1→L2 就绪≥70、L2→L3 就绪≥90+运行史≥5)逐级不跨级、降档信号(熔断 tripped/连败/漂移)安全优先，消费 #36 熔断 + #37 就绪分/漂移**零改其核心**；level set 跨级拒 exit2、--confirm 才落盘；48 测试。收编接线：均自接线（kernel index re-export、program 已注册）。六门全绿：vitest 1452+2skip、hooks 180、adapters 58、verify-skills OK、bundle 7/7、oracle 0 不一致。**GOAL B19/D16 勾——loop-engineering 治理闭环完成（registry→enforce→budget→drift→audit→graduation 全链，两竞品都无此面）；A3 dashboard 里程碑全收编。** 遗留：dashboard-app types.ts 前端 phase 边镜像（#25b 只点名 cli/server，另开后续）。 |
| 2026-07-07 | iteration-22 | 并行批次 11 收编（#39 适配器框架 + #36 loop 预算熔断）→ M7 + M-loop 推进 | #39：adapters/ 框架——contract.md 三能力（inject←session-start / veto←gate / track←skill-tracker）+ 分档降级 A/B/C，registry.yaml 能力矩阵单源（active: claude/codex(A)/cursor(B)；planned: gemini/copilot/pi/devin 已登记目标档），**tools/test-adapters.sh 58 conformance 断言**（归一 canonical 决策 + 反例哨兵 + **真适配器变异测试**：改坏 codex veto 立即抓红、还原回绿——把老仓"contract 是约定不是测试"变机器约束）；**Cursor 转正**（老仓 spike 的"声明 track 却不写 history"病灶修复：track 透传 baseline 真写、inject 如实降级 .cursor/rules）。#36：loops budget.ts——token 级 budget + circuit breaker（ok/warn/tripped 超阈值 exit2）+ 成本估算（cadence×pattern），只给 LoopBudget 加 2 可选字段、enforce 零改动；28+16 测试。收编接线：均无需 packages 接线（loops 已注册/导出；adapters 自足）。六门全绿：vitest 1237+2skip、hooks 180、**adapters 58**、verify-skills OK、bundle 7/7、oracle 0 不一致。GOAL B15/B20 勾、D7 平台策略面推进。 |
| 2026-07-07 | iteration-20 | 并行批次 9 收编（#29 AFK Sandcastle + #34 tap 核心）→ M5+M8 起步 | 两大子系统新包并行（#29 第一 agent API 中断早退无残留、已重起）。#34：新 @pipeline-lite/tap（node stdlib 零第三方）——daemon 多端口（8766 claude 生命线隔离有测试）+ capture/forward proxy（透明转发+录制）+ trace_store（JSONL 侧文件替 SQLite，B3 一致）；**安全护栏三条全落地**（默认 OFF 真不监听、本地不外发=源码级扫描断言四模块零 outbound 网络 import、状态可见 tapStatus）；49 例含 13 真 socket。#29：新 @pipeline-lite/automation——队列 8 态生命周期状态机（off→queued→running→merged/failed/conflict/paused，cas 并发闸走 kernel）+ scheduler 信号量并发 + lifecycle 挂队/沙箱/merge-back 编排；**L1→L3 合体**（L1/L2→paused 不自动 merge 安全、L3→merged）；72 例 + 2 docker honest-skip（无 docker 不伪绿）；**tsc 抓出 fake 测试掩盖的真 bug**（scheduler 误调自由函数被 vitest fake 静默吞）。收编接线：根 build 纳入 tap+automation、npm install 建软链、**doctor 接 tapStatus 加 security:tap 灯（#34e 收编前置：敏感能力明示）**、doctor 测试更新 11 项 + tap 灯真/黄用例。留后续 #29c docker 全链/#29d server afk 数据端/#29e 毕业制、#34b ws/CA/#34c 多 runtime/#34d traffic 查看器。五门全绿：vitest 1117/1117+2skip、hooks 180、前端 71、verify-skills OK、bundle 7/7、oracle 0 不一致。 |
| 2026-07-07 | iteration-19 | 并行批次 8 收编（#26 前端重构 + #35 loop 治理）+ 主会话 #26c server 服务 SPA | #26：全新 dashboard-app（Vite+React18+TS），**老仓 UI 四病灶全解**——① 看板只做操作（无配置矩阵）② 收件箱作默认落地视图（答"哪个 change 在等我决定"）③ 一级导航恰好 3 项（收件箱/看板/设置）+ debug 降级为 Advanced 折叠 ④ statusline 已在终端（#10）；深浅色+中英 i18n；71 真 render 测试（jsdom 真 DOM + 真拖拽 fireEvent + SSE 真 EventSource emit→组件更新）+ 3 真 server HTTP 集成。#35：loop 治理（老仓 loops_registry/enforce 移植）——registry 窄解析+JSON-Schema 子集校验、R1-R11 裁决（KILL_RULES={R1,R2,R4,R6}、verdict 取严 reasons 全列）、**L1→L3 分级放权入 schema**（缺省 L1 report-only，即便 kill 命中也只报告——loop-engineering 核心），15 真 fs 例。**主会话补 #26c**：server 加 webRoot——GET / 真返 SPA index.html+token 注入 </head>、GET /assets/* 真静态供给+路径穿越防护，server.test +1 真 HTTP。收编接线：loops barrel/桥/program、根 build:web/test:web 脚本、npm install 建 dashboard-app 软链。五门全绿：根 vitest 994/994（+72）、前端 jsdom 71/71、hooks 180、verify-skills OK、bundle 7/7、oracle 0 不一致。**GOAL B10-B12 UI 病灶全勾、B18 loop 治理勾、D9 dashboard 勾。** |
| 2026-07-07 | iteration-18 | 并行批次 7 收编（#25 dashboard server + #27 channel）→ M3+M4 推进 | 两大新包并行。#25：新 @pipeline-lite/server 包（node stdlib http 零第三方），端点 health(+版本)/snapshot(聚合全项目 kernel 读)/stream(SSE mtime 指纹)/POST transition；**架构欠账修复 B4 版本抢占**（探测 health 版本 → 旧版本 SIGTERM 优雅让位，真进程 e2e 实证）+ **B5 token 鉴权**（crypto 256-bit 一次性 token + 0600 握手文件 + timingSafeEqual 常量时间比较，POST 无 token 真 401）；38 例含 17 真 HTTP。#27：channel event-sourced worker 总线（append-only events.jsonl + 从事件流纯函数投影 registry/forum/thread/turns/guard；seq 侧车 reconcile 永以 jsonl 为真相不留空洞；进程管理层 supervisor/真 spawn 留后续明确标注、CLI 遇 spawn/kill exit 2），143 测试含 12 真 fs；**barrier 隔离红线三重自证**（throwing-proxy 挂 deps.store/flow + channelDir 物理隔离 + integration 跑完零 openspec/门 marker mtime 不变）。收编接线：kernel barrel export channel、桥 import 改回、program 注册 channel、根 build 纳入 packages/server、npm install 建 server 软链。发现技术债 #25b（server 镜像 cli transition 事件表=重复真相源，待上提 kernel 单源）。五门全绿：vitest 922/922（+181）、hooks 180、verify-skills OK、bundle 7/7、oracle 0 不一致。 |
| 2026-07-07 | iteration-17 | 并行批次 6 收编（#21 PostToolUse 全套 + #28 mem）→ **M2 收官 + M4 起步** | 两 agent 文件不相交并行。#21：四 PostToolUse hook 纯 bash（confirm-clear 清门 marker、decision-recorder 记 AskUserQuestion 进 JSONL kind=prompt、skill-tracker 记 Skill 进 JSONL kind=tool、interactive-skill-gate 注入交互硬姿态+落 interaction marker）；JSON 转义硬测（换行/反斜杠/引号注入仍恰一行合法 JSONL）；红线自证零解释器；test-hooks +64→180。#28：mem 跨 runtime 会话检索（Claude ~/.claude/projects、Codex ~/.codex/sessions、Pi ~/.pi 三格式全量解析 + list/search/context/extract/projects/phase 切片 + compaction/cwd 作用域/时间窗），12 真 fs 例（真建 fixture session→真检索评分排序）；OpenCode SQLite 诚实降级待补（G5 登记）。收编接线：kernel barrel export mem、桥 import 改回、program 注册 mem 命令（--allowUnknownOption 让 cmdMem 自解析 flag）。五门全绿：vitest 741/741（+95）、hooks 180/180、verify-skills OK、bundle 7/7、oracle 0 不一致。**🎉 M2 hooks/插件全保真里程碑收官**（六项 #19-#24）。GOAL A2 勾选。下一批 M3 dashboard / 继续 M4。 |
| 2026-07-07 | iteration-15 | 并行批次 4 收编（#16 living-spec + #17 session）→ **M1 收官** | 两 agent 并行。#16：老仓 state-spec.sh 逐字盘点 specs/set-spec-scope/inject-jsonl（扁平 capability 枚举、spec_scope 标量 CSV/哨兵、jsonl 注入 cat+目录展开），14 真 fs 例。#17：session activate（落 .pipeline-active 平指针）/ route-context（monorepo 包路由），9 真 fs 例；**诚实处理**老仓 3 个 [PLACEHOLDER] 空占位 + session_store 持久化缺口——照实标注待移植、不伪造实现（GOAL C 诚实门）。收编接线：barrel 导出 spec+session、桥 import 改回 @pipeline-lite/kernel、program 注册 spec/session 命令、integration 补走 buildProgram 的 spec/session 真 e2e、.gitignore 补运行时产物。五门全绿：vitest 541/541（+109）、hooks 84/84、verify-skills OK、bundle 7/7、oracle 0 不一致。**🎉 M1 内核深度里程碑收官**——七项（guard/TTL/transition/task/spec/session/manifest）全收编，GOAL A1 勾选。下一里程碑 M2（hooks/插件全保真），首项 #19 router 消费 #18 就绪的 manifest 派生。 |
| 2026-07-07 | iteration-13 | 并行批次 2 收编（#14 transition 全副作用 + #13 门 TTL 分级） | 两 agent 文件不相交并行。#14：老仓 state-transition.sh case 块逐字盘点，补齐 explore/spec/build/verify 出口全前置校验（design_doc/plan/build_mode/isolation/direct_override/report/branch_status/双review/barrier）+ history raw=事件名，ERROR 文案逐字对齐；transition-effects.integration.test.ts 17 真 fs 例。#13：门 TTL 恢复分级（confirm 300s / review·interaction 1800s，GATE_TTL_MS 单源），gate/statusline/session-start/inbox/doctor 五处同步，inbox-ttl.integration.test.ts 7 真 fs 例。**收编时真测试暴露 2 处欠具体的旧 integration 用例**（裸跑 transition 未喂前置——老内核本就 exit 1 拒绝），改为喂足真实前置的忠实全程。修 CONTRACT §2 TTL 分级 + §3 删 2 条已消除白名单。五门全绿：vitest 351/351（+40）、hooks 84/84、verify-skills OK、bundle 7/7、oracle 0 不一致。遗留入 backlog：lifecycle hooks（#18/M2）、automation 入队（#29b/M5）。 |
| 2026-07-07 | iteration-11 | 并行批次 1 收编（#20 + #22/#23 + #26b） | 三 agent 并行、收编点统一验门。#20 SessionStart 三注入（宪法 templates/workflow.md + 上下文 + openspec，纯 bash fail-open，test-hooks +18）；#22/#23 skills/agents 内容面（14 skills + 4 agents + commands 骨架，脚本引用改写为新 CLI，零悬空引用）；#26b `pipeline doctor` 健康面（降级可见，--json schema）。收编时命中两处 agent 写盘竞态假红（verify-skills/test-hooks），重跑确认全绿。五门全绿：vitest 292/292（+18）、hooks 73/73、verify-skills OK、bundle 7/7、oracle 0 不一致。**用户 AFK 指令**：全程不打断，跑到 kill 判据。**范围再扩**：M5 AFK Sandcastle（#29–#29e）+ M8 tap（#34–#34e）确认全量迁移。 |
| 2026-07-06 | iteration-9 | 收敛检查（v0.1 kill 判据第 1 轮） | **v0.1 四项成功判据全部达成 ✅**：①oracle 双跑 0 不一致 ②交叉验证过——lite 连写（set/cas/import --strip）后老内核 get/status 正常读取 ③vitest 232/232 + kernel 零运行时依赖 ④bundle 全程 open→archive 七相位一遍跑通（archived=true）。**同轮收到用户范围升级指令：全部核心功能迁移 + 历史缺口全解决 → 不收官，转 v1.0**。GOAL 增 v1.0 六里程碑（M1 内核深度→M6 竞品缺口），BACKLOG 编入 #12–#34 共 23 项，LOOP budget 修正允许轮内并行 agent。队首：#12 guard 全量校验面。 |
| 2026-07-06 | iteration-8 | BACKLOG #11 老仓导入工具 | 收编：TDD 先红后绿。kernel `parseLegacyHistory/stripLegacyHistory`（tools/prompts/transitions 三节、b64 正则预校验、坏行 fail-open）+ CLI `pipeline import <name> [--strip]`（import 哨兵幂等、append fail-loud）。JSONL kind 加法扩展 tool/prompt/import + raw（CONTRACT §1 已注）。真跑老仓 fixture：43 条导入、YAML 历史节清空、状态仍可读、重跑安全。五门全绿：vitest 232/232（+9）、hooks 55/55、bundle 7/7、verify-skills OK、oracle 0 不一致。**队列清空** → 下轮进收敛检查（kill 判据第 1 条计数开始）。 |
| 2026-07-06 | iteration-7 | BACKLOG #10 statusline | 收编：TDD 先红 1 后绿。hooks/statusline.sh 纯 bash（json_get 同 gate、grep 提顶层键、mtime 取最新活跃 change、新鲜门 marker 显示 等:<kind>、archived 隐藏、fail-open 空输出）；纳入零解释器红线自证名单；README 接入说明。四门全绿：hooks 55/55、vitest 223/223、verify-skills OK、oracle 0 不一致。队首：#11 老仓导入工具（队列最后一项）。 |
| 2026-07-06 | iteration-6 | BACKLOG #9b `inbox --html` 静态单页 | 收编：TDD 先红 2 后绿。自足单页（零外部资源断言、深浅色、HTML 注入转义、空态、生成时间快照标注）。五门全绿：vitest 223/223（+3）、hooks 47/47、bundle 7/7、verify-skills OK、oracle 0 不一致。队首：#10 statusline。 |
| 2026-07-06 | iteration-5 | BACKLOG #9 切片 + #9a `pipeline inbox` | 收编：#9（2d）按 budget 切为 9a/9b。9a 落地：三门 marker（新鲜判定同 gate TTL）+ 复核相位停留 change 双源合并、等待时长降序、`--json` schema 稳定、同名去重 marker 优先；端到端真跑（init→transition→inbox 一屏）。五门全绿：vitest 220/220（+6）、hooks 47/47、bundle 7/7、verify-skills OK、oracle 0 不一致。诚实记录：本轮测试与实现同批落盘、未跑显式红（协议瑕疵，下轮回归先红纪律）。队首：#9b 静态 HTML 单页。 |
| 2026-07-06 | iteration-4 | BACKLOG #8 esbuild 单文件分发 | 收编：`npm run build` 产出自足 pipeline.mjs（147KB，commander/kernel 全内联、shebang 保留、chmod +x），bin 指向 bundle，`npx pipeline` 上手路径进 README。新增 tools/test-bundle.sh 冒烟 7/7（真跑 init→get→transition→history）。五门全绿：vitest 214/214、hooks 47/47、verify-skills OK、bundle 7/7、oracle 0 不一致。队首推进至 #9（收件箱 UI，2d 超单项预算，下轮按 LOOP budget 切片）。 |
| 2026-07-06 | iteration-3 | BACKLOG #7b PIPELINE_AFK=1 逃生门 | 收编：gate.sh 顶部显式 AFK 放行（仅字面 "1"，不清 marker）。TDD 先红 1 后绿；四门：hooks 47/47、vitest 214/214、verify-skills OK、oracle 0 不一致。队首推进至 #8（esbuild 单文件分发）。 |
| 2026-07-06 | iteration-2 | BACKLOG #7 history JSONL 收尾 | 收编：kernel 出 `createHistoryWriter`（fs appender 从 main.ts 收编进 kernel）；CLI set/set-many/cas/init 补 best-effort 记账（transition 已有，无双写）。TDD 先红 5 → 全绿；四门：build 零错、vitest 214/214、hooks 44/44 + verify-skills OK、oracle 双跑 0 不一致。队首推进至 #7b（AFK 逃生门）。 |
| 2026-07-06 | iteration-1 | T2–T7 五模块并行移植 + 集成回归门 | 收编：build 零错；vitest 207/207 + oracle 套件 9/9；test-hooks 44/44 + verify-skills OK；**oracle 双跑 0 不一致**（含 base64 历史区 PRESERVE）。契约表按实测回写（init/get/transition），有意差异白名单 4 项见 CONTRACT §3。遗留：kernel 侧 HistoryWriter 未实现（CLI transition 已记 JSONL，set/init 未记）；PIPELINE_AFK 逃生门未移植；chat track 无老内核对照。 |
