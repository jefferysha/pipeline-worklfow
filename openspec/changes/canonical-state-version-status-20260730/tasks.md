# 任务

## 立项

- [x] 建立新 Change、固定目标与非目标。
- [x] 由 `openspec-propose` 登记 proposal、design 与 tasks 的真实证据。

## 调研

- [x] 固定五个上游版本、源码与差异映射。
- [x] 审计本仓 canonical decoder、snapshot、Dashboard 调用方与非重复边界。
- [x] 形成技术设计、ADR 与风险裁决。

## 规格

- [x] 编写 `canonical-state-version-status` delta spec 与可执行计划。
- [x] 补齐全栈 coverage，明确向后兼容、错误/空/加载状态和验证矩阵。
- [x] 根据第二次 Verify finding 修订有界截断、Machine sibling 与本地化通用重试契约。

## 实现

- [x] TDD 打通 future-version typed error、snapshot issue 与 Progress notice 曳光弹。
- [x] 收敛 kernel/server 畸形输入、mixed-project、路径不泄露与稳定排序边界。
- [x] 完成 Dashboard strict decoder、zh/en、loading/empty/error/retry 与 Onboarding 优先级。
- [x] 运行定向测试、全量构建/测试和 Build pre-Verify convergence。
- [x] 以 TDD 覆盖 101 项结构化截断，保持 compatibility-only 项目与 readable sibling 可导航。
- [x] 修正 Machine 风险分流，并完成双语截断提示。
- [x] 本地化 snapshot 请求失败，提供复用现有 refresh 的通用 retry。
- [x] 更新公共契约，重跑全量门禁与独立 Build convergence。
- [x] 在非 Progress 视图保留旧 snapshot 时展示本地化错误与通用 retry，解码失败不伪报 HTTP 200。
- [x] Retry pending 期间保留 alert 并禁用按钮，重跑全量门禁与生产浏览器验收。

## 验证

- [x] 冻结 build SHA，完成 Reviewer/Codex/E2E/视觉轨且前后 workspace fingerprint 一致。
- [x] 在真实 Tenon Dashboard 以 1440×900、1024×768 验收身份、中英文、升级/加载/空/错误和键盘路径。
- [x] 一次性登记 verification report；可行动 finding 全部清零并记录已接受的 Low 残余。

## 交付

- [x] 提交并 push 唯一分支，创建包含五个上游固定证据的非草稿 PR。
- [x] 检查并修复可行动 CI 失败，记录外部阻塞。

## 归档

- [ ] 应用 delta spec，使用 Tenon CLI 完成 archive 并回写 automation memory。
