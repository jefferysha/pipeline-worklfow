# Tasks

## Open

- [x] Bind a new `backend/default` Change with the strict `full` preset.
- [x] Record the architecture-audit problem, scope, non-goals, and acceptance
  signal without pre-deciding the implementation.
- [x] Capture an initial composable-policy hypothesis and the questions Explore
  must answer.

## Explore

- [x] Inventory every place that couples Workflow names/shape, Track policy,
  documents, Skills, gates, automation, persistence, and UI projections.
- [x] Convert every applicable `AGENTS.md`, COMMON, FRONTEND, and BACKEND clause
  into a rule-compliance matrix with source evidence and an enforcement method.
- [x] Audit package dependency direction, DDD ownership, DTO/persistence/API
  boundaries, frontend domain architecture, strict typing, file-size limits,
  security invariants, generated assets, and verification reality.
- [x] Build an evidence-backed issue matrix separating confirmed defects,
  rule violations, intentional invariants, documented exceptions,
  compatibility constraints, and unrelated debt.
- [x] Compare viable architecture options and record the selected decisions,
  trade-offs, ADR, and Superpowers design.

## Spec

- [x] Convert every approved issue into precise delta requirements and
  scenarios, including invalid combinations and legacy compatibility.
- [x] Define schema, compiler, persistence, CLI/API/UI, bundle, and migration
  contracts with an implementation plan.
- [x] Specify enforceable repository-architecture checks for confirmed Agent
  Rule violations without duplicating production behavior.
- [x] Replace the Build checklist below with concrete file- and test-level work
  derived from the approved specification.

## Build

- [x] Add effective-plan characterization and a three-step governed
  tracer-bullet through kernel, CLI, server, document evidence, and Todo.
- [x] Implement the versioned declarative governance profile, legacy
  `required` alias, profile persistence/projection, and invalid-combination
  tests.
- [x] Make all Workflows use current-visit Skill policy and exact selected-root
  evidence; centralize prompt intent and exact-context bare-continue approval.
- [x] Split server routes, decode request DTOs, move loop state reads behind a
  repository/codec, and replace copied automation policy with public exports.
- [x] Decompose every backend hard-limit production violation by bounded
  context/use case while preserving public facades and behavior.
- [x] Restore frontend dependency direction, split the API client with runtime
  decoders, and make Todo/progress/skill views consume effective-plan data.
- [x] Decompose every frontend hard-limit component/page violation while
  preserving i18n, accessibility, interaction states, responsive behavior,
  and themes.
- [x] Eliminate unsafe assertions within every touched production boundary and
  add malformed-input coverage.
- [x] Add `check:architecture` for objective Agent Rules with exact justified
  exceptions, wire it into root scripts/CI, and clear all blockers.
- [x] Synchronize schemas, templates, docs, dashboard DTOs, tracked bundles,
  Skills, and immutable install verification with source.
- [x] Bind Codex transcript reconciliation to the exact selected plugin root
  and current step visit, including physical symlink containment.
- [x] Make the persisted document profile an immutable runtime constraint for
  transition, CLI document, and server projection consumers.
- [x] Reject declarative document reads unless the owner dominates every graph
  path to the reader.
- [x] Close every Workflow DTO object over known keys and reject unknown HTTP
  fields without writing a definition.
- [x] Remove remaining runtime capability branches on the `default` name and
  make the architecture checker prove the EffectiveWorkflowPlan boundary.
- [x] Bind document read receipts to an immutable current step-visit identity
  and reject cross-visit replay even when the digest is unchanged.
- [x] Bind Codex Skill evidence to the exact current visit and completed host
  call across selected cache, managed-release, and direct-development roots.
- [x] Make EffectiveWorkflowPlan carry the complete Track, automation, Skill,
  document, review, transition, and projection capabilities consumed at runtime.
- [x] Persist exact legacy/default document-governance profile fingerprints and
  fail closed when an established Change's mutable definition drifts.
- [x] Make the architecture checker detect every reserved `default` comparison
  alias and permit each reviewed compatibility occurrence only once.
- [x] Upgrade partial legacy WorkflowRun identities at the explicit document
  migration boundary without overwriting established governance bindings.
- [x] Reject Codex transcript evidence unless the exact nested exec completed
  successfully and the trusted Skill read was structurally reachable.
- [x] Make structured non-zero exits authoritative over stdout text and reject
  mixed shell control flow that can skip a nominal Skill read.
- [x] Restrict transcript command evidence to complete allowlisted read-only
  Skill sequences so early successful shell termination cannot authorize.
- [x] Route default and custom host Skill calls through the same effective-plan
  gate, and extend architecture scanning to production hooks and runtime.

## Verify

- [x] Run focused tests and independent architecture/code review.
- [x] Re-run the complete Agent Rule compliance matrix against the final tree
  and resolve every blocker or document a valid rule-owned exception.
- [x] Run full build/test plus hook, adapter, Skill, bundle, freshness, comment,
  and workflow-oracle gates.
- [x] Verify real CLI/API/dashboard behavior for short, default, simple, free,
  and custom governed workflows.

## Ship

- [x] Apply approved delta specifications to canonical OpenSpec.
- [x] Synchronize user, architecture, compatibility, and release documentation.
- [x] Prepare a clean, evidence-backed delivery without unrelated changes.

## Archive

- [ ] Re-read all registered evidence, archive the completed Change, and verify
  it no longer appears as active.
