# Tasks

## Open

- [x] Record the independent problem, scope, and acceptance signals.
- [x] Capture the DDD boundary decision and compatibility/security constraints.

## Explore

- [x] Trace the effective Track Registry through the kernel encoder, Bash parser, and root skill.
- [x] Record the cache-version and interaction decision in the Superpowers design and ADR.

## Spec

- [x] Define the normal-chat routing delta and testable invariants.
- [x] Write the implementation plan before changing the router contract.

## Build

- [x] Extend the router projection/cache/parser with the default workflow binding.
- [x] Emit selection metadata only for new objectives with project custom choices.
- [x] Update the root pipeline skill and focused regression tests.
- [x] Require selection for a built-in Track whose effective workflow is non-default, with a regression test.

## Verify

- [x] Run focused kernel, CLI, hook, and dashboard tests plus release-bundle validation.
- [x] Run fresh-install and browser/Codex live acceptance; retain evidence in the isolated project.

## Ship

- [x] Build the release bundle and update the installed test plugin from `main`.
- [x] Verify generated documents/ledger reads before release handoff.

## Archive

- [x] Archive only after live default, custom, and AFK flows have all been verified.
