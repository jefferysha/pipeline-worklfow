# Issue #46 interaction observability · Verify attempt 2

## 结论

**FAIL（C0 / H1 / M2 / L0）**。这是 Change
`issue-46-interaction-events-metrics` 的第 2/2 次、也是最后一次正式 Review。冻结候选为：

- branch：`codex/issue-46-interaction-metrics`
- base：`d58df7a0ecbb155d54d81e782150bf68567cb617`
- HEAD / `build_sha`：`6cf44730294aa51e06fa4c5ac509e198214c4568`
- tree：`c655280334b911daca106d68a2dda93c56d8c7d3`
- candidate：`workspace:sha256:c7f77c3b18c6ec56e3c37418f875d54c01b15c9f35983db6a596da7fc320fe81`
- attempt id：`1b9862f4-f32b-4f88-9a95-ceba7e4128d7`

根代理逐项复核 frozen diff、issue Acceptance/Measurement、attempt 1 findings 与 worker
修复后，确认 replay 的终态顺序验证仍未闭合；standards/spec 轨另确认 canonical review
binding 的安全读取与规格兼容性缺口。E2E/spec-isolation 证据轨通过，但不能覆盖这些静态反例。
本报告不授权 `verify-pass`；Review 预算已耗尽，不进入第三次 Build/Review，不创建 PR。

## 最终阻断 findings

### HIGH

1. **Replay 对 terminal 后的非法核心事件静默跳过，没有产生要求的 `malformed-order`。**
   `packages/kernel/src/interaction/replay.ts:178-181` 在 stale/rejected/failed/valid-resume
   终态后，对除重复成功 resume 外的所有 known core events 直接 `continue`。因此
   `request → ack(rejected stale) → ack(success) → effect(success) → resume(success)` 虽不会再被
   误计为 completion，却也不会报告后续 success chain 的非法回退；当前
   `packages/kernel/src/interaction/replay.test.ts:85-112` 只断言 `validResume=false`，没有断言
   `malformed-order`。这部分重开 attempt 1 的 H2：终态已经阻止假绿，但仍没有成为严格、可诊断的
   state machine。它直接违反 delta spec `:123-145` 的 deterministic replay/乱序要求和 issue
   “malformed ordering detectable and tested” 验收项。修复方向应为：终态后仅显式允许幂等 resume；
   其他 known core event 一律产生 `malformed-order` 并保持非完成，同时覆盖 request/ack/effect 的
   重复与回退组合。

### MEDIUM

1. **Authorization-critical review binding 使用 TOCTOU 且无界的 reader。**
   `packages/kernel/src/state/review-gate-binding.ts:89-105` 先 `lstat(target)`，随后通过另一次
   `readFile(target)` 打开路径；两次操作间可被替换为 symlink，且读取前没有 byte 上限。
   该 sidecar 直接参与 acknowledge/transition 授权，不能只依赖事前 path metadata。
   应复用 bounded `O_NOFOLLOW` regular-file handle、打开前后 identity/size fence 与稳定脱敏错误，
   并增加 symlink replacement、oversize 测试。

2. **实际 canonical review 行为超出了已批准 OpenSpec 的兼容性边界。**
   proposal `:20-26` 声明“现有 review 与 canonical state 行为保持兼容，仅增加派生观测投影”，
   且旧 Change 缺 projection 保持原行为；但实现新增 `.pipeline-review-gate-binding.json` 作为
   authorization sidecar，`packages/cli/src/commands/review.integration.test.ts:196-208` 明确要求
   legacy pending/approved receipt 缺 binding 时 fail-closed。该防护可能是正确修复，但它不是派生
   interaction projection，也改变了既有 receipt 的可消费行为。Build 中发现语义变化后未走
   `requirements-changed` 返回 Spec；正式规格仍缺 sidecar schema、迁移/恢复、fail-closed 与安全
   reader 要求，因此 spec lane 不能通过。

## Review lanes

| lane | 结果 | 证据 |
| --- | --- | --- |
| 根代理 code review | FAIL | 亲自检查 replay、binding reader、review tests、proposal/delta spec，并复现静态反例 |
| standards | FAIL | 独立只读审查确认 terminal 静默跳过、binding TOCTOU/无界读取、canonical 兼容性漂移 |
| spec | FAIL | 实现改变 canonical receipt 授权行为，但批准 proposal/delta 未登记该契约 |
| e2e | PASS | exact candidate 身份、生产/dist 字节、fixture scorecard 与隔离 OpenSpec 证据均一致 |

standards 子轨在给出上述三个可定位 finding 后被根代理按“有限 Review”约束停止礼貌性等待；
不以等待超时清除已返回 finding，也没有启动替代 reviewer 或重置 attempt 计数。

## Issue #46 Acceptance / Measurement

| 验收项 | attempt 2 |
| --- | --- |
| versioned privacy-minimal envelope、closed codec、扩展 codes | PASS |
| request/ack/effect/resume 正常 trace 与 canonical state anchors | PASS |
| raw prompts/secrets/credentials/artifacts 不进入 | PASS |
| append-only JSONL、跨进程 sequence/hash、bounded projection reader | PASS |
| 三项 scorecard 与 benchmark fixtures | PASS |
| projection loss、物理乱序、state discontinuity | PASS |
| terminal 后非法重复/回退产生 malformed ordering | **FAIL** |
| completeness ≥99%、accepted stale=0、same-state repeat=0 | tracked cohort 数值通过；validator 反例阻止最终验收 |
| 已批准 Spec 与 canonical review 兼容面一致 | **FAIL** |

## 验证证据（复用 exact-candidate 原始记录）

按编排约束，本 attempt 没有重跑全仓、clean-install、build 或 bundle：

- 产品修复后的唯一一次全仓运行：`6745 passed / 27 skipped / 4 failed`；四个 failure 均为旧测试
  fixture 直接伪造 approved receipt，不符合新 binding 契约。
- 只修改两个测试文件后：受影响两文件 `32/32`，interaction focused 十文件 `157/157`。
- `npx tsc -b packages/kernel packages/cli --pretty false`、architecture 864 production files、
  `npm run build`、dist freshness、bundle smoke `32/32` 均通过。
- `15d22c8c… → b168b75c…` 只改变
  `packages/cli/src/transition-afk.integration.test.ts` 与
  `packages/cli/src/commands/advance.test.ts`；CLI/server dist blobs 均未变化。
- built scorecard artifact SHA-256：
  `5314655e8e6f1bfaa9a8e25e59ea3c437f8b9dfda6521d2dc564712fae72ca56`；GCR `0.6`，
  interruptions/completion `1.6666666666666667`，median resume `4000ms`，completeness `1`，
  accepted stale `0`，same-state repeat `0`，invalid resume `1`。
- OpenSpec isolated apply/archive 证据字节未变；applied main spec SHA-256：
  `bd38a3e52dc9288db3c0cf3c79a458ba7abfbea16e48774447ea832d76623007`。
- 27 个 skip 是 Docker daemon 不可用及 `TENON_REQUIRE_REAL_CODEX!=1` 的既有环境/CI gate；
  本 Change 无 Dashboard UI/server API，browser QA 不适用。

`docs/TEST-REALITY.md:105-113` 仍描述较早候选的 6729/27/0 全仓证据；本报告以上述最终候选的
6745/27/4 + test-only targeted 收口为权威记录。Verify 中没有回写 Build 文档或实现。

## 已知非阻断事实

1. ledger-bound plan/spec 的 EOF 空行会令 `git diff --check` 报既有 whitespace warning；为保持已登记
   digest，不在 Verify 手改或伪造重登记。
2. 旧 Codex read-only sandbox 的 EPERM、model-cache、analytics/transcript 工具警告不是产品失败。

## 收口边界

本 attempt 登记为 `standards=fail / spec=fail / e2e=pass`，aggregate 为 FAIL。Review 已用
`2/2`；按照 issue 执行契约，不进行第三次修复、复审或完整测试门。保留 branch、exact HEAD、
candidate fingerprint、lane receipts 与本报告供后续治理决策；不 push、不创建 PR、不 merge、
不发布版本。
