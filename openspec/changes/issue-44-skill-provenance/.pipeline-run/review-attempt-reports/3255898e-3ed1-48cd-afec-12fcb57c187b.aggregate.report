{"version":1,"attemptId":"3255898e-3ed1-48cd-afec-12fcb57c187b","candidateFingerprint":"workspace:sha256:cda0ed3b7fd8675d0a3f88b2fc9267b65631ab957e2abeeb788145aaea0f1a98","requiredLanes":["standards","spec","e2e"],"lanes":[{"lane":"standards","result":"fail","reportPath":"openspec/changes/issue-44-skill-provenance/.pipeline-run/review-attempt-reports/3255898e-3ed1-48cd-afec-12fcb57c187b.standards.report","reportDigest":"sha256:c271c710afc20f38d0414583082ece4759260fd633bbd3be54c5eb33831c81fe","recordedAt":"2026-08-09T19:23:04Z"},{"lane":"spec","result":"fail","reportPath":"openspec/changes/issue-44-skill-provenance/.pipeline-run/review-attempt-reports/3255898e-3ed1-48cd-afec-12fcb57c187b.spec.report","reportDigest":"sha256:c271c710afc20f38d0414583082ece4759260fd633bbd3be54c5eb33831c81fe","recordedAt":"2026-08-09T19:23:08Z"},{"lane":"e2e","result":"pass","reportPath":"openspec/changes/issue-44-skill-provenance/.pipeline-run/review-attempt-reports/3255898e-3ed1-48cd-afec-12fcb57c187b.e2e.report","reportDigest":"sha256:c271c710afc20f38d0414583082ece4759260fd633bbd3be54c5eb33831c81fe","recordedAt":"2026-08-09T19:23:09Z"}],"result":"fail","sourceReport":"docs/superpowers/reports/2026-08-10-issue-44-skill-provenance-verify-attempt-2.md"}
# Issue #44 Skill provenance — Verify attempt 2

## 结论

- Change：`issue-44-skill-provenance`
- Review attempt：`3255898e-3ed1-48cd-afec-12fcb57c187b`（2/2，预算已耗尽）
- 冻结候选：`c38694185a35a376b096c1d3c131e389dc981094`
- 候选 tree：`fe7c3c6d3bf79d549ab5200d68c67da9210732a9`
- 集成基线：`origin/main=d58df7a0ecbb155d54d81e782150bf68567cb617`，祖先检查 exit 0
- Tenon candidate fingerprint：`workspace:sha256:cda0ed3b7fd8675d0a3f88b2fc9267b65631ab957e2abeeb788145aaea0f1a98`
- 聚合结论：**FAIL / BLOCKED**。E2E lane 通过，但 standards/spec lane 确认 3 个 P1 与 2 个 P2；不得进入 `verify-pass`，不得自动修复或开始第 3 次 Review。

Attempt 1 的完整 23 项 gate、full Vitest、full release-store、`check:npx-package` 已在冻结旧候选上执行并记录。本 attempt 按编排约束只运行 post-integration 候选的风险匹配窄门，没有把旧结果冒充当前执行，也没有重复完整门。

## Lane 结果

| Lane | 结果 | 证据 |
| --- | --- | --- |
| standards | **FAIL** | 根代理逐项复核 + 独立 standards reviewer；128/128 changed paths 纳入审查，确认 P1=3、P2=2。 |
| spec | **FAIL** | Findings 违反 `plugin-distribution` 的冻结 executable、`skill-provenance` 的 exact physical tree / stable category，以及设计中的 cross-platform containment/TOCTOU 承诺。 |
| e2e | PASS | exact-commit 隔离 clone 的 51/51 selected tests、14 类真实负向 fixture、62-entry measurement、fresh sync 与 OpenSpec rehearsal 全部通过。 |

Codex CLI 是可选降级轨，本轮记为 **unavailable**，不形成额外 lane：第一次调用因参数组合错误未进入审查；第二次因 runtime/model/cache 错误未产生 verdict，精确进程终止为 exit 130。按有限 Review 约束不再更换调用形式或重试，不把它伪装为 PASS/FAIL，也不形成隐性第 3 次 Review。临时证据根：`/tmp/tenon-issue44-codex-review.LpfRKh`。

## 根代理确认的 findings

### P1 — provenance Bash delegation 未在每次 Node spawn 前 replay 冻结 binding

`packages/cli/src/commands/packaged-assets.ts` 与 `packages/cli/src/commands/update-candidate-verification.ts` 只读取 Node binding 的 `.executable` 并作为 `--node` pathname 传入。`bindNativeHostCommand()` 看到的实际 command 是 Bash，因此只 replay Bash binding；`tools/verify-skills.sh` 随后只验证 Node pathname 为绝对且可执行，直接 spawn，未证明它仍是冻结的物理对象。

同一缺口存在于 `RuntimeReleaseStore` 的 runner wrapper；`setupSkills.ts` exact-root gate 直接使用 `process.execPath`，doctor 路径只重新解析 PATH。只有 `inspectCandidatePayload()` 的专用 wrapper 在 Bash verifier spawn 前同时调用 `verifyNode`，不能替其他生产调用链补证。

这使 attempt 1 的 frozen-node finding 保持 OPEN，并违反主规格 `plugin-distribution` 的“每次 spawn 前重验冻结 executable，不得回退 `process.execPath`、裸 PATH 或未绑定 pathname”。

待修边界：把 Bash provenance delegation 明确建模为 Bash + Node 两份 binding，在 package/update/release-store/setup/doctor 的每次 verifier spawn 前同时 replay；补 proof-before-spawn 顺序测试。

### P1 — Skill root containment 检查与实际哈希未绑定同一 inode

`packages/automation/src/skills/skill-provenance.ts::assertPhysicalSkillRoot()` 会拒绝静态顶层 symlink，但不把 child/root 的 dev/ino snapshot 交给 `buildCanonicalManifest()`。后者重新 `realpath()` 并建立新的 root identity；两步之间目录若被替换，实际哈希可以绑定到另一个树。`packages/cli/src/skill-provenance-locator.ts` 同样只比较 realpath 字符串，没有证明 containment 检查与遍历读取的是同一物理对象。

待修边界：由共享 canonical manifest 原语接收并前后复验 skills root + child root 的 dev/ino/realpath snapshot，locator 与 filesystem verifier 复用；增加 deterministic root-identity replacement fixture。

### P1 — canonical hasher 的 Windows containment 使用固定 POSIX 分隔符

`packages/automation/src/skills/snapshot-manifest.ts` 的三处 `relative()` containment 只检查 `../`。Windows 返回 `..\\...`，因此内部 link/junction 的逃逸不会被该条件拒绝；issue #44 新 verifier 与 locator 都依赖这一 canonical hasher。

待修边界：统一复用 `sep` + `isAbsolute()` 的 `pathWithin()`，并在真实 Windows runner 覆盖内部 link/junction 逃逸。

### P2 — strict `source_ref` 安全谓词弱于共享 Skill ID 契约

`packages/kernel/src/skills/source-registry.ts::isSafeProvenanceId()` 只拒绝恰好为 `..`，但共享 `isPathSafeSkillId()` 拒绝任何含 `..` 的 id。诸如 `a..b` 可先通过 strict registry parser，再由 hasher 以 `filesystem-safety-error` 拒绝，破坏 `invalid-source-ref` 的前置、稳定 category。

待修边界：在低层公开唯一安全谓词供 kernel parser、automation 与 locator 共用，增加 `invalid-source-ref` fixture。

### P2 — `sync --json` 把不同错误统一误标为 filesystem safety

`packages/cli/src/commands/internal-skill-provenance.ts` 的 sync catch 对 unsupported version、unknown source、invalid source ref、full-set 与 IO 错误都输出 `filesystem-safety-error`。这违反 internal CLI machine-readable category 稳定契约。

待修边界：保留 `SkillProvenanceRegistryError.category`，为 authoring/full-set/IO 使用明确 typed category，并补 JSON 断言。

## Attempt 1 findings 回归状态

| Finding group | Attempt 2 状态 |
| --- | --- |
| Frozen Node/PATH | **OPEN**：传递 pathname，但未在全部生产调用链 replay binding。 |
| 顶层 symlink/root containment | **PARTIAL**：静态 Skill/root symlink 已拒绝；root identity TOCTOU 与 Windows containment 未关闭。 |
| Sync parent identity / CAS / fsync | CLOSED。 |
| Missing/empty bundled root no-fallback | CLOSED。 |
| Unsafe caller ID | 调用参数已前置拒绝；canonical `source_ref` 判据仍 PARTIAL。 |
| Admission error mapping | CLOSED。 |
| LF normalization | CLOSED。 |
| Fresh-clone sync prerequisites | CLOSED。 |
| Full physical set + independent candidate verification | CLOSED。 |
| Duplicate/trailing/legacy strict parsing | CLOSED。 |
| Success-only locator cache | CLOSED。 |
| Any legacy node rejected | CLOSED。 |
| Real-repository measurements | CLOSED。 |

## 当前候选的验证证据

E2E/spec 证据根：`/tmp/tenon-issue44-attempt2-e2e.amzJK6`；权威汇总：`FINAL-SUMMARY.txt`。Source/clone HEAD 都是 `c38694185a35a376b096c1d3c131e389dc981094`，隔离 clone 最终 clean。Source fingerprint 前后均为 `workspace:sha256:cda0ed3b7fd8675d0a3f88b2fc9267b65631ab957e2abeeb788145aaea0f1a98`，真实工作区只保留本 Change 的 Tenon governance paths。

- `npm ci`：exit 0，486 packages，0 vulnerabilities；5 个 allow-scripts warning，不是 blocker。
- 定向 prerequisites：`npx tsc -b packages/kernel packages/channel packages/tap packages/automation packages/cli` exit 0。
- Focused provenance：5 files / 44 tests passed。
- Runner no-lower-tier：4/4 selected passed；49 selector-filtered。
- Release-store：3/3 selected passed；83 selector-filtered；覆盖 candidate drift 保持 active/launchers、legacy lock 拒绝、fully verified rollback。
- 本 attempt 合计：7 files，51/51 selected tests passed，0 runtime skip；132 项是 `-t` selector-filtered，不计真实 skip。
- CLI/shell clean、JSON、quiet、显式 frozen Node、fake PATH：全部预期退出；quiet stdout/stderr 0 bytes；fake-node invocation 0 bytes。
- 14 类真实负向 fixture 全部 exit 1 且命中预期 category：missing、malformed/duplicate version、extra、duplicate source、trailing、unsafe id、legacy、coordinate、content/mode drift、top-level Skill symlink、bundled `skills/` root symlink。
- Sync 两次 exit 0 且 0 bytes；registry SHA `f0e5be7b3a42133f24b82fa1c72abdc2b04542c165461c78e1bd62a4a8014dfc`、mode `644` 不变。
- Measurement：62 physical / 62 registry / 62 verified hashes / 62 valid coordinates / 0 findings；legacy absent；额外 tracked provenance source 0。
- Fresh exact clone 的 documented sync exit 0，最终 HEAD/tree exact、diff/status clean。
- OpenSpec：3 specs / 9 deltas；strict change 1/1；隔离 archive 应用 9 requirements；strict all 43/43，三个目标规格各 1/1。
- Source 与原 clone 主规格 digest 前后保持 `tree-sha256:74a27188dd95b808ef480fa2337264ae351d0df0aef2f3dc1e834a990856366e`；只有 rehearsal 副本变为 `tree-sha256:b1d73357863fbf2160e131796239cbeda21db1a03b56ab9b5b7e5bd25786713a`。
- 整个 `--root` 的 lexical symlink alias 会被 direct verifier 接受；根代理确认它不是 attempt 1 finding 或三份 delta 的独立要求，candidate/payload 层另有 symlinked payload 门，因此作为非阻断观察保留。

没有运行：attempt 1 已执行的完整 23 项矩阵、full Vitest、full release-store、`check:npx-package` 未在 attempt 2 重复；这是明确的编排约束，不冒充当前 PASS。

## Issue Acceptance / Measurement 判定

| Acceptance / Measurement | 当前证据 | 判定 |
| --- | --- | --- |
| 一个 canonical provenance model 驱动 install/verify/doctor/bundle | v3 registry 已接入全部路径，legacy lock 已移除 | 功能面满足，但受 P1 executable/root identity 缺口阻断最终验收 |
| 每个 distributed Skill 有 source/hash/coordinate | 62/62/62，coordinate 62 | PASS |
| Legacy lock 安全移除并持续禁止 | absent；candidate/fixture 以 `legacy-provenance-source` 拒绝 | PASS |
| drift/unknown/hash mismatch fail closed 且诊断可操作 | 当前 Linux fixtures 全覆盖；Windows containment 与 sync JSON category 仍不满足 | **FAIL** |
| clean/drifted/legacy/rollback tests | 当前 selected tests 与 attempt 1 full gate 有证据 | PASS，但不能覆盖上述未测信任边界 |
| 用户文档匹配 runtime model | README/CONTRIBUTING/CONTRACT/DIST-RELEASE 已同步 | PASS |
| Unconsumed tracked provenance sources | 0 | PASS |
| Distributed Skills without verified hashes | 0 | PASS |
| Fixture drift detection | 已声明 fixtures 为 100%；但缺 root replacement/Windows fixture，不能据此覆盖发现的语义缺口 | **FAIL** |

## Step 1.5 — 128/128 changed paths 对照规格

根代理与独立 reviewer 均按 `origin/main...c3869418` 审查 128/128 paths；独立 reviewer 逐项审 57 个实现/测试/规格/文档/dist 路径，并核对 71 个 Tenon governance/evidence 路径的冻结身份与 attempt 归属。以下分组覆盖列表中的每一项且计数合计 128：

| Path group | 数量 | 回读 capability / contract | 结论 |
| --- | ---: | --- | --- |
| `.gitattributes`、根 package、README/CONTRIBUTING、CONTRACT/DIST-RELEASE | 6 | `skill-provenance`、`plugin-distribution`、`repository-architecture-compliance` | 已回读；文档/生成前置满足 |
| Issue #44 ADR/design/plan/reports 与完整 active Change / Tenon canonical evidence | 82 | 三份 delta、`declarative-document-governance`、`review-attempt-budget`、`interaction-and-skill-provenance` | 已回读；canonical evidence 仅由官方 CLI 产生 |
| hook/install/templates/tools/legacy lock 删除 | 6 | `skill-provenance`、`plugin-distribution`、`interaction-and-skill-provenance` | 已回读；发现 frozen Node binding P1 |
| automation source/tests/admission | 5 | `skill-provenance`、`skill-content-resolution` | 已回读；发现 root identity 与 Windows containment P1 |
| CLI source/tests + tracked CLI dist | 24 | 三份 delta、`plugin-distribution`、`repository-architecture-compliance` | 已回读；发现 Node binding 与 stable JSON category 缺口 |
| kernel parser/export/tests | 3 | `skill-provenance`、`repository-architecture-compliance` | 已回读；发现共享 safe-id 判据 P2 |
| server dist / marketplace test | 2 | `plugin-distribution`、`repository-architecture-compliance` | 已回读；freshness/build 证据存在 |

同时回读了受影响主规格：`plugin-distribution`、`skill-content-resolution`、`versioned-plugin-release-lifecycle`、`repository-architecture-compliance`、`review-attempt-budget`、`declarative-document-governance`、`interaction-and-skill-provenance`，以及 Change 的三份 delta。因为上述 findings 仍违反规格，本 HARD gate 的结果是 FAIL，不得勾选 Verify tasks 或进入 Ship。

## 交接 / 停止条件

- 保留候选 commit `c38694185a35a376b096c1d3c131e389dc981094` 与本报告；不创建 PR、不 push 新验收状态、不运行 exact-head CI，因为候选不可合并。
- 完成本 attempt 的 `standards=fail`、`spec=fail`、`e2e=pass` 与 aggregate `fail` 后，请求 exact `verify-fail` delegated receipt 并通过官方 transition 回到 Build。
- Review budget 为 `used=2/max=2`；回到 Build 仅表示 canonical failed state，不授权自动修复或第三次 Review。后续必须由人类显式处置 review budget/需求或终止 Change。
- 待修范围严格限于本报告 5 个 findings；Codex CLI unavailable lane 不再重试。
