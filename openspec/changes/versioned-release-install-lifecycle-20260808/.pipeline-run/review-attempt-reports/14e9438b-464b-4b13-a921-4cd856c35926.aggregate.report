{"version":1,"attemptId":"14e9438b-464b-4b13-a921-4cd856c35926","candidateFingerprint":"workspace:sha256:a40bea5f1cf9e24d45da45569c5f9ea066dd0f8297ab17ff0c2a02e5cec27c09","requiredLanes":["standards","spec","e2e"],"lanes":[{"lane":"standards","result":"fail","reportPath":"openspec/changes/versioned-release-install-lifecycle-20260808/.pipeline-run/review-attempt-reports/14e9438b-464b-4b13-a921-4cd856c35926.standards.report","reportDigest":"sha256:c143a7144747eaa8a8fe66bfb56d0f8fcf46e6ae69e8d511a8b76cb684361aa4","recordedAt":"2026-08-09T06:59:25Z"},{"lane":"spec","result":"fail","reportPath":"openspec/changes/versioned-release-install-lifecycle-20260808/.pipeline-run/review-attempt-reports/14e9438b-464b-4b13-a921-4cd856c35926.spec.report","reportDigest":"sha256:c143a7144747eaa8a8fe66bfb56d0f8fcf46e6ae69e8d511a8b76cb684361aa4","recordedAt":"2026-08-09T06:59:25Z"},{"lane":"e2e","result":"fail","reportPath":"openspec/changes/versioned-release-install-lifecycle-20260808/.pipeline-run/review-attempt-reports/14e9438b-464b-4b13-a921-4cd856c35926.e2e.report","reportDigest":"sha256:c143a7144747eaa8a8fe66bfb56d0f8fcf46e6ae69e8d511a8b76cb684361aa4","recordedAt":"2026-08-09T06:59:26Z"}],"result":"fail","sourceReport":"docs/superpowers/reports/2026-08-09-versioned-release-install-lifecycle-20260808-verify-attempt-1.md"}
# 版本化发布安装生命周期 · Verify Attempt 1

## 结论

Attempt `14e9438b-464b-4b13-a921-4cd856c35926` 审查了唯一冻结候选
`workspace:sha256:a40bea5f1cf9e24d45da45569c5f9ea066dd0f8297ab17ff0c2a02e5cec27c09`，
聚合 `standards`、`spec`、`e2e` 三条 lane，占用已配置的两次上限中的第 1 次。结果为
**fail**。该候选不得进入 PR、合并、tag、公开 Release、真实插件卸载或公网重装。

范围严格冻结在实施计划 R01–R15；以下只登记矩阵内缺口和本候选的直接回归，不继续扩展相邻改进。

## 发布阻断

### 1. R15 — Review attempt 未绑定当前冻结 build 候选（High）

`review-attempt begin` 接受任意形状合法的 `--candidate`，context 没有从当前 review step 的冻结
`build_sha` 推导目标；`requireActiveReviewAttempt` 也只核对 lane。调用方可以让 Verify 冻结在候选 A，
却为候选 B 开 attempt，然后在错误候选下派发 Review Skill/E2E。

Build 必须从当前 review step 声明的冻结输入推导 candidate，在 `begin` 精确比较，并在每次 Review lane
派发前由 internal Skill gate 再次比较。补 candidate mismatch 与 resumed attempt 测试。

### 2. R06/R07 — public/native host-mutation lock 没有共用可证明 owner 协议（High）

公开 installer 写 `.pipeline.lock/owner.json`，native kernel lock 读取 `.pipeline.lock/owner`。native 因而把
存活的公开 owner 判断为 unknown，并回退到锁目录年龄；目录超过 60 秒后，即使公开 PID 存活且
`owner.json` 心跳新鲜，native 仍可回收。两侧 release 也仍是 read-owner 后递归删除，不是 fenced/CAS。

Build 必须统一 owner record 与 heartbeat 真相源，禁止回收已证明存活的 owner，并保证旧 owner 释放时
绝不删除 successor。新增缺失的反向 barrier：native 在 stale 阈值后仍必须拒绝 live public installer，
并保留已有 public-refuses-native 覆盖。

### 3. R02 — workflow snapshot 兼容、生成投影与真实路由未收敛（High）

新的 legacy Review-lane projection 在恢复旧 V1/V2 时生效，但 V1 的一条历史 fingerprint 路径仍对投影
后的字节求 hash。真实冻结 V1 default snapshot 被拒绝：
`expected=c9a829... historical=de42f8...`。

真实 v1.0.1 bundle gate 还要求旧 CLI 读取未来 V3 snapshot。这与优先级更高的 R02 冲突：新 Run 保持
V3；兼容方向应是新 CLI 继续读取/修改 V1/V2，而不是不可变 v1.0.1 理解未来 schema。门禁应改成用
真实 v1.0.1 创建 legacy state，再证明当前 CLI 可读写。

default workflow generated source 与 pinned router contract 同时陈旧；router revision mismatch 让真实 hook
集成测试返回空 dispatch。Kernel/server/CLI 也保留 pre-V3、pre-`kind` 断言，native-host/Doctor fake 未实现
新增 tag-object proof 命令。

Build 必须修正 V1 hash-before-projection 顺序，保持当前 default Run V3，修正 N−1 方向，重新生成
default/router 投影，并更新全部 closed-shape fixture。

### 4. R01 — 受控发布资产陈旧（High）

隔离 `npm run build` 改写两份 bundle：

- CLI：`16fa87dd...` → `c91836a6...`
- server：`1ea5cbff...` → `7adf4321...`

Dashboard dist 在 build 前还同时包含新旧 hash chunk；build 删除了旧文件。Build 必须基于最终源码重建
CLI/server/Dashboard，并证明连续两次 build 字节一致、scoped candidate diff 为零。

### 5. R03 — local clean-install 没有提供 exact Release proof fixture（High）

installer 正确地在 host mutation 前拒绝尚未发布的 v1.0.2 GitHub Release；但 local clean-install 仍直接访问
公网 API，没有本地 exact Release/tag-object fixture，因此发布前验收固定以 HTTP 404 结束。

Build 必须扩展隔离 fixture，模拟精确 Release 与 tag object identity，同时保留生产 fail-closed 行为；不得
削弱公开 installer，也不得伪装公网 Release 已存在。

## Medium

### 6. R15 — 仅修改 Review 次数时策略区保存按钮保持 disabled

真实 production Dashboard QA 中，复制 workflow 后把“最大 Review 次数”从 2 改为 3，全局顶部保存可以
成功持久化，但策略区本地保存仍 disabled。`policyDirty` 只比较 decomposition/interaction，遗漏
`reviewBudget`。需把 budget 纳入 dirty predicate，并补“只改次数即可启用并保存”的回归测试。

## Lane 证据

### Standards — fail

- 静态代码/安全审查确认 Finding 1、2、4、6。
- `npm run check:architecture` 通过：844 个生产文件、5 个已声明 size-only exception。
- identity、release-workflow、docs、repository-hygiene、legacy-bridge 通过。
- R03/R04/R05/R08/R09/R10/R11/R12/R13/R14 在冻结矩阵内未发现其他 C/H/M。

### Spec — fail

- Change strict validate：1/1 通过。
- 隔离 archive rehearsal：26 个 delta，新增 21、修改 5；archive 后 all strict：42/42 通过。
- 真实 `openspec/specs` 未改变。
- Finding 1–3、5 仍违反冻结 candidate、shared lock、V1/V2 兼容和 exact Release acceptance 契约。
- 演练目录：`/tmp/tenon-openspec-rehearsal.23UAiS`。

### E2E — fail

- Core：381 files；369 pass / 12 fail；6,662 tests pass / 19 fail / 27 skip。
- 11 个失败文件串行复现：274 pass / 18 fail。release-store crash matrix 单独复跑 59.46 秒通过，故全套
  timeout 记为容量噪声，不登记为产品 finding。
- Web：97 pass / 1 fail；1,739 tests pass / 1 fail。失败是 `reviewBudget` 进入 definition 后的陈旧 payload
  断言。
- npm bootstrap 66/66、OpenSpec 41/41、identity/release/docs/architecture/hygiene/legacy bridge 通过。
- default-workflow freshness 有真实 diff；真实 N−1 payload/digest 准备通过，但失败于 Finding 3 的两个
  future-V3 断言。
- local clean-install 在零 mutation 前因 Finding 5 的 v1.0.2 Release 404 失败。
- 原始日志：`/tmp/tenon-verify-attempt1.DPUSta/logs`、
  `/tmp/tenon-verify-attempt1-git.s1sLrp`。

## 浏览器验收

在隔离 runtime 中走查 production Dashboard 的桌面与 320 px：default workflow 只读；副本可编辑；budget=3
可通过全局保存持久化；320 px 横向 overflow=0；注入 workflow API 失败后出现本地化 alert 与 retry；初始
console error=0。策略区保存缺陷见 Finding 6。隔离 server 已停止，无残留 listener。

仓库外截图：

- `/tmp/tenon-browser-qa.gixjWK/workbench-initial.png`
- `/tmp/tenon-browser-qa.gixjWK/workbench-review-budget-header-saved.png`
- `/tmp/tenon-browser-qa.gixjWK/workbench-review-budget-320.png`

## Changed-file 覆盖映射

| 变更面 | 审查能力 |
| --- | --- |
| `install.sh`、installer/clean-install tools | R03/R04/R06/R07/R09/R10 |
| CLI setup/update/stable-release/host/doctor/dashboard/review-attempt | immutable target、host transaction、Dashboard identity、有限 Review |
| CLI runtime 与 `runtime/tenon-bootstrap.mjs` | R05/R08 activation、launcher、rollback、audit |
| kernel state/workflow 与 workflow templates | R02/R15 snapshot、policy、lane、budget、跨进程状态 |
| Dashboard/server source | review budget UX、API projection、Dashboard readiness |
| release workflows、identity/docs tools | tag-only release、串行公网验收、Release evidence |
| README/docs/OpenSpec/Skills | R13/R14 安装、恢复、卸载与治理生命周期 |
| 受控 `dist/**` | R01 新用户预构建 payload |

## 下一轮 Build 闭集

一次性修复上述六组问题、全部红色 closed-shape 测试与生成投影；依次跑定向测试、full core/web/build、
N−1、local clean-install、identity/release/docs/OpenSpec。Build 再次冻结后才能开始 Attempt 2。Attempt 2 是
配置允许的最后一次 Review，不得新增相邻范围。
